# Compatibility

Compatibility proof is not complete.

| Workflow | Status | Evidence |
| --- | --- | --- |
| Native Rust project policy | Partial | `tests/project_policy.rs` rejects frontend-runtime sentinel files, JavaScript/TypeScript source files, and Rust dependencies that would add Electron, webview, browser, or JavaScript runtime surfaces, verifies the library and binary crate roots keep `#![forbid(unsafe_code)]`, guards that CI keeps running the documented root checks, guards required repository-readiness docs/community files, and guards the required issue-label taxonomy; this guards native-only/safe-code/repository-check architecture rules but does not prove live terminal behavior |
| ANSI SGR colors and text attributes | Partial | `tests/parser.rs` covers ANSI colors, semicolon and colon-form indexed/truecolor colors including colon-form truecolor with an optional color-space slot, semicolon-delimited extended colors before grouped SGR params, out-of-range extended color rejection without leaking rejected truecolor components into later attributes, invalid grouped and semicolon extended-color mode rejection, bold, dim, italic, inverse video, slow/rapid blink, hidden/conceal, framed, encircled, overline, strikethrough, SGR 21 double underline, colon-form underline single/double/curly/dotted/dashed/reset styles without italic side effects, ignoring unsupported colon-form underline styles, SGR underline color/reset, bounded underline-color metadata table overflow to default color, and reset behavior |
| Cursor movement, erase-line, erase-display, wrapping, and reset | Partial | `tests/parser.rs` and `tests/terminal_state.rs` cover cursor movement, erase-line, visible-screen erase-display modes 0/1/2, scrollback-clearing mode 3, DEC autowrap/origin modes, RIS reset of visible state, scrollback, modes, margins, style, and cursor, and DECSTR soft reset of modeled modes including autowrap without clearing visible content |
| Terminal status and capability reports | Partial | `tests/terminal_state.rs` covers Primary DA `CSI c`/`CSI 0c`, DECID `ESC Z`, Secondary DA `CSI > c`/`CSI > 0c`, DSR `CSI 5n`, regular cursor-position `CSI 6n`, DEC private cursor-position `CSI ? 6n`, DEC private printer/UDK/keyboard/locator reports `CSI ? 15n`/`25n`/`26n`/`53n`, terminal parameter reports `CSI x`/`CSI 1x`, ANSI mode-state reports `CSI 4 $ p`/unrecognized-mode replies, DEC private mode-state reports `CSI ? 1 $ p`/`CSI ? 6 $ p`/`CSI ? 7 $ p`/`CSI ? 12 $ p`/`CSI ? 25 $ p`/`CSI ? 47 $ p`/`CSI ? 66 $ p`/`CSI ? 1000 $ p`/`CSI ? 1002 $ p`/`CSI ? 1003 $ p`/`CSI ? 1004 $ p`/`CSI ? 1006 $ p`/`CSI ? 1047 $ p`/`CSI ? 1049 $ p`/`CSI ? 2004 $ p`/unrecognized-mode replies, DEC private save/restore for focus-report mode, DECRQSS SGR `DCS $ q m ST` including framed/encircled attributes and underline color, scroll-margin `DCS $ q r ST`, and cursor-shape `DCS $ q SP q ST` status strings plus invalid-query and oversized-query rejection, xterm window-state report `CSI 11t`, deterministic origin window-position report `CSI 13t`, xterm pixel-size report `CSI 14t` from configured native dimensions, xterm text-area size report `CSI 18t`, and core-dimension screen-size report `CSI 19t` response bytes; `tests/osc_and_paste.rs` covers xterm window icon-label report `CSI 20t` and window-title report `CSI 21t`; `tests/app.rs` covers native runtime write-back of terminal-generated responses to the PTY and resize propagation for pixel-size reports; `tests/cli.rs` covers `--runtime-response-smoke` pumping cursor/status/primary-DA/secondary-DA queries through `NativeTerminalRuntime` and verifying retained PTY response writeback without GPU bootstrap |
| VT editing/navigation subset | Partial | `tests/vt_editing.rs` and `tests/terminal_state.rs` cover default and configurable tab stops, C0 LF/VT/FF column-preserving linefeed controls, ANSI linefeed/newline mode, C1 8-bit IND/NEL/HTS/RI/DECID equivalents, DEC special graphics G0/G1 box-drawing charsets with SI/SO shifting, DECALN screen alignment pattern, cursor forward/backward tabs, insert/delete/erase/repeat characters including repeat after combining marks and emoji modifiers plus split wide-cell metadata repair, insert/replace mode, insert/delete lines with scroll-margin bounds and no-op behavior outside scroll margins, viewport scroll up/down including the ECMA-48 scroll-down alias with scroll-margin bounds, DECSTBM linefeed, index/next-line, and reverse-index scroll margins, cursor absolute/relative row and column positioning including DEC origin-mode margin clamps for relative vertical motion, cursor next/previous line movement, cursor visibility/blink mode, DECSCUSR cursor shape/blink state, DEC cursor/rendition save/restore, SCO/private-mode cursor/rendition save/restore, and DEC private mode save/restore for modeled modes including autowrap, application keypad, and bracketed paste |
| OSC title, hyperlink, and clipboard handling | Partial | `tests/osc_and_paste.rs` covers OSC 0 icon-label/title updates, OSC 1 icon-label-only updates, OSC 2 title-only updates, overlong title/icon-label rejection without clearing previous state, bounded OSC 8 hyperlink metadata on printed cells including ignoring overlong URIs without clearing the active link and bounded hyperlink-table overflow to unlinked cells, and bounded OSC 52 clipboard-text decoding including encoded and decoded size-limit rejection; `tests/app.rs` covers host clipboard abstraction sync; `cargo run -- --osc52-clipboard-smoke` on 2026-06-23 decoded 18 OSC 52 bytes and wrote them through the native OS text clipboard |
| Bracketed paste mode | Partial | `tests/osc_and_paste.rs` covers plain paste before enablement, bracketed wrapping after `DECSET 2004`, multiline UTF-8 payload preservation, disablement, and DEC private save/restore of bracketed paste state |
| Unicode wide and combining characters | Partial | `tests/terminal_state.rs` covers wide-cell leading/trailing metadata, wide-character metadata at the right edge with autowrap disabled, combining marks attached to wide glyphs and right-edge cells, stacked combining marks retained on one base cell, emoji modifier sequences, variation-selector emoji presentation clusters, keycap emoji clusters, regional-indicator flag pairs including a right-edge cluster boundary, tag-sequence emoji flags, simple plus multi-part emoji ZWJ sequences, rainbow-flag ZWJ clusters, emoji-width widening for ZWJ sequences that start from narrow symbol clusters, emoji-modifier ZWJ sequences including modifiers on later joined components, multi-part ZWJ sequences with multiple skin-tone modifiers, and ZWJ emoji sequences with internal variation selectors retained as one wide-cell cluster; `tests/reflow.rs` covers regional-indicator, tag-sequence emoji flag, rainbow-flag ZWJ, multi-part ZWJ, emoji-modifier ZWJ, modifier-on-joined-component, multi-modifier ZWJ, and internal-variation-selector ZWJ cluster metadata across visible-grid resize reflow; `tests/render_plan.rs` covers stacked combining-mark cell text, multi-codepoint ZWJ, multi-modifier ZWJ, rainbow-flag ZWJ, internal-variation-selector ZWJ, and tag-sequence emoji flag text preservation through renderer planning; `tests/selection.rs` covers copying emoji-modifier ZWJ, rainbow-flag ZWJ, internal-variation-selector ZWJ, and tag-sequence emoji clusters once across leading/trailing cells; broader grapheme-cluster shaping coverage pending |
| Bounded scrollback | Partial | `tests/terminal_state.rs` covers bounded scrollback append, long-output eviction capped to the configured scrollback limit while preserving recent rows, rich style/hyperlink/underline-color and wide-cell metadata when rows scroll offscreen, and erase-display mode 3 scrollback clearing; `tests/scrollback_view.rs` covers core scrollback viewport navigation through retained history, live-grid restoration, cursor hiding while viewing history, screenshot rendering of displayed history rows, and scrollback-view performance plus dirty-viewport accounting; `tests/cli.rs` covers `--runtime-scrollback-smoke` pumping deterministic runtime output, using Shift+PageUp/PageDown through `NativeTerminalRuntime`, rendering scrolled history and restored live content, and verifying local scrollback navigation writes no PTY input |
| Visible-grid and scrollback resize reflow | Partial | `tests/reflow.rs` covers visible-grid soft-wrap/hard-newline/style/hyperlink/underline-color/wide-cell reflow and styled scrollback row reflow including wide-cluster, hyperlink, and underline-color metadata |
| Dirty-region tracking | Partial | `tests/dirty_regions.rs` covers printable spans, erase-line spans, resize viewport invalidation, full-viewport scroll invalidation, scroll-margin linefeed and reverse-index invalidation, repaired wide-cell insert/delete/erase edit invalidation, and property-tested coalescing/containment/drain invariants across arbitrary bounded region sequences; `cargo bench` includes `dirty_region_coalescing` for overlapping span/cell/region coalescing, containment, and drain behavior |
| Configuration validation and files | Partial | `tests/config.rs` covers default config validity, zero terminal dimensions, oversized visible-grid area rejection before allocation, bounded frame targets, finite bounded font-size validation, rounded renderer font-size mapping, shell program/args/cwd parsing and validation, partial TOML config defaults, validated TOML parsing from strings/files, malformed TOML errors, missing-file errors, pretty TOML serialization roundtrip, deterministic config-file reload checks, changed valid config application, and previous-config preservation when a changed file is invalid; `tests/cli.rs` covers `--config-template` printing a parseable starter TOML without GPU bootstrap, `--config-check <path>` validating TOML config files, `--config <path>` launching through the native app boundary with mapped frame target, runtime dimensions, scrollback settings, configured shell command, renderer dirty-region mode, rounded renderer font size, and config-file reload path retention, `--runtime-config-reload-smoke` polling an unchanged config file, applying a changed config to terminal dimensions, scrollback, frame cadence, renderer dirty-region/font settings, and shell command without GPU bootstrap, and invalid launch config reporting without GPU bootstrap or app launch; `tests/app.rs` covers mapping validated `GromaqConfig` into native app frame targets, runtime terminal dimensions, scrollback limits, shell command settings, explicit native app runtime construction, explicit native app renderer configuration, reloadable native terminal/frame/render setting application without restarting the PTY runtime, reloadable shell setting application before runtime start, retained PTY session restart when the shell command changes, and native app config-file polling for changed reloadable terminal/frame/render/shell settings; `tests/wgpu_renderer.rs` covers configured renderer font size reaching planned glyph cache keys and renderer reconfiguration affecting future frame planning; `tests/glyph_atlas.rs` covers zero and oversized glyph atlas capacity rejection before cache-slot allocation; broader live desktop config-reload proof pending |
| Reproducible benchmark documentation | Partial | `documentation/benchmarks.md` documents the local verification sequence, expected Criterion benchmark names, regression handling, font dependency boundary, and live-performance non-claims |
| Golden terminal-state fixtures | Partial | `tests/golden_fixtures.rs` compares terminal output against `tests/fixtures/terminal_golden/ansi_scrollback_alternate.txt`, `tests/fixtures/terminal_golden/vt_unicode_osc.txt`, `tests/fixtures/terminal_golden/vt_editing_status.txt`, `tests/fixtures/terminal_golden/osc_clipboard_paste.txt`, and `tests/fixtures/terminal_golden/status_capability_reports.txt`, covering ANSI styling, OSC 8 hyperlink metadata, OSC title/icon-label responses, OSC 52 clipboard state, bracketed paste encoding, wide-cell and complex emoji-cluster state, cursor editing, tab-stop behavior, insert/delete editing, DEC special graphics, grouped terminal-generated status/capability/window responses, scrollback, cursor state, performance counters, and alternate-screen restoration; `tests/fixtures/README.md` documents the fixture inventory, update workflow, and review rules; broader reference-terminal fixture coverage pending |
| Structured performance counters | Partial | `tests/test_api.rs` covers parser bytes, dirty cells, non-empty dirty-region batches, scrolls, and resizes through `TerminalTestApi::dump_perf_metrics`; `tests/app.rs` covers `NativeTerminalRuntime::dump_runtime_perf_metrics` for PTY output batches/bytes, terminal response writes/bytes, app input writes/bytes, native key inputs, paste/committed-text bytes, resize events, render attempts, rendered frames, rendered dirty regions/cells/max cells, clean-frame skips, measured render-time sample/total/average/max nanoseconds, bounded bucketed render-time p95 estimates, bounded app-input-to-render latency sample/total/average/max/p95 estimates, and `NativeTerminalRuntime::dump_runtime_state_snapshot` visible-cell plus retained scrollback row/cell/cap counters; `tests/cli.rs` covers `--runtime-perf-smoke` reporting rendered dirty-region work plus render and input-to-render sample/average/max/p95 metrics, `--runtime-large-output-smoke` reporting pumped bytes plus rendered frames and viewport-capped rendered dirty-region work, `--runtime-bounded-state-smoke` reporting retained scrollback rows/cells against an explicit maximum retained-cell cap from the runtime state snapshot plus viewport-capped rendered dirty-region work, `--runtime-continuous-output-smoke` reporting streamed PTY batches, rendered dirty frames, scrollback cap behavior, viewport-capped rendered dirty-region work, and render p95 estimates, `--runtime-scrollback-smoke` reporting local scroll rows, rendered frames, rendered dirty-region work, and zero PTY input writes for local history navigation, `--runtime-response-smoke` reporting terminal response writes/bytes separately from PTY input writes, and `--runtime-idle-smoke` reporting clean-frame skips without GPU bootstrap; `cargo bench` includes `runtime_protocol_input_reports` for deterministic focus-report, SGR mouse-report, and terminal-response counter paths; `cargo run -- --runtime-perf-smoke` on 2026-06-23 pumped 1 byte, rendered 1 frame, and reported rendered dirty-region work plus render and input-to-render sample/average/max/p95 counters through the deterministic CPU-side path; `cargo run -- --runtime-continuous-output-smoke` on 2026-06-23 pumped 6912 bytes across 32 batches, rendered 32 CPU-side frames, capped scrollback to 64 lines, and reported render p95 500000 ns; `cargo run -- --runtime-idle-smoke` on 2026-06-23 pumped 0 bytes, reported 16 render attempts, 16 clean-frame skips, and 0 rendered frames through the deterministic CPU-side path; live p95 frame-time/input-latency/CPU/process-memory acceptance proof pending |
| Structured test API screenshots | Partial | `tests/test_api.rs` covers deterministic one-pixel-per-cell RGBA screenshots of visible terminal text and cursor state; live GPU/window screenshot proof pending |
| Selection/copy | Partial | `tests/selection.rs` covers single-cell, reversed, multi-row, displayed scrollback viewport row copying, soft-wrap newline omission, hard-break newline preservation, wide-cell text copying including selections that begin on a wide-cell trailing half, emoji-modifier ZWJ cluster, and tag-sequence emoji flag text copying, selected blank-cell preservation before later text, explicit selection clearing, stale selection clearing across resize and alternate-screen transitions, and viewport-clamped selection ranges |
| Host clipboard adapter | Partial | `tests/clipboard.rs` covers copy into deterministic memory clipboard and native OS adapter construction; `tests/app.rs` covers native runtime selection copy into a host clipboard adapter plus Super+C, Control+Shift+C, Control+Insert, and dedicated Copy shortcut recognition without stealing plain Control+C or plain Insert from shells; `tests/cli.rs` covers `--clipboard-smoke` success, readback mismatch, no-prior-text cleanup, stale-sentinel cleanup paths, and `--runtime-clipboard-paste-smoke` routing clipboard text through `NativeTerminalRuntime` into the retained PTY session with previous text restored; `cargo run -- --clipboard-smoke` on 2026-06-23 round-tripped 22 bytes through the native OS text clipboard; `cargo run -- --runtime-clipboard-paste-smoke` on 2026-06-23 routed 30 pasted bytes through the runtime paste path, reported 1 clipboard paste, and restored previous readable clipboard text |
| Alternate screen | Partial | `tests/alternate_screen.rs` covers primary grid/cursor restoration, `1049` cursor/rendition save and restore, repeated `1049` enter preserving the original primary cursor, inactive `1049` exit not restoring unrelated saved cursors, alternate-screen entry clearing an active scrollback view, resizing while alternate-screen is active and reflowing the saved primary buffer before restore, and suppressing alternate-screen output from scrollback; `tests/app.rs` covers native runtime alternate-screen output plus SGR mouse press, release, drag, wheel, and any-motion reporting to the retained PTY session; `tests/cli.rs` covers `--runtime-alternate-screen-smoke` pumping primary output, alternate-screen output, and alternate-screen exit through `NativeTerminalRuntime`, rendering each dirty stage, proving primary content restoration, and proving alternate-screen output stays out of scrollback; `cargo run -- --runtime-alternate-screen-smoke` on 2026-06-23 pumped 42 bytes across 3 stages, rendered 3 CPU-side frames, restored `primary`/`restored` visible content, and suppressed alternate-screen scrollback |
| Default and SGR mouse press/release/drag/motion reporting | Partial | `tests/mouse.rs` covers xterm default and SGR mouse press/release/wheel encodings, keyboard modifier bits, drag and any-motion modes, default-protocol coordinate bounds, SGR coordinate overflow rejection, disabling behavior, independent `1000`/`1002`/`1003` reset interactions, DEC private save/restore for mouse reporting modes and SGR protocol, and preserving button reporting when motion modes are disabled; `tests/app.rs` covers native window-coordinate SGR mouse press, release, drag, and any-motion reporting while an alternate screen is active |
| `/bin/sh` PTY command | Partial | `tests/pty.rs` covers short command spawn/readback, interactive shell input written through the PTY master, output drained through the background reader, and clean child exit |
| Native `winit` application lifecycle | Partial | `tests/app.rs` covers window attributes, resume, timed PTY pump deadlines, PTY output-ready user events, output-driven redraw scheduling, dirty terminal frame rendering through the renderer boundary, structured renderer-error propagation without counting failed frames as rendered while keeping the frame dirty for retry, close, and destroy lifecycle state |
| No-argument native app launch dispatch | Partial | `tests/cli.rs` verifies CLI dispatch to the native app launcher without invoking GPU smoke paths and verifies `--config <path>` launches through the same native app boundary with validated config-derived settings |
| Native app shell PTY startup | Partial | `tests/app.rs` verifies runtime terminal dimensions, PTY config, single shell spawn, and retained session handle |
| Native app PTY I/O pump/input/resize | Partial | `tests/app.rs` verifies drained PTY output reaches terminal state, terminal-generated status responses are written back to the PTY, schedules redraw when output is applied, syncs terminal-owned clipboard text through the host clipboard abstraction, maps native window resize events to terminal/PTY sizes, maps native window mouse coordinates and keyboard modifiers to grid mouse reports while rejecting out-of-window and non-finite coordinates, tracks pressed native mouse buttons for drag versus plain motion including active left/middle/right priority and ignored pseudo-buttons, maps window drag, any-motion, and alternate-screen SGR press/release/drag/any-motion reports to PTY bytes, scrolls local scrollback on non-reporting wheel input and Shift+PageUp/PageDown without writing PTY bytes, leaves primary-screen Shift+PageUp unhandled without forwarding PTY bytes when local scrollback cannot move, sends Shift+PageUp to alternate-screen apps when local scrollback cannot move, resizes terminal state and the retained PTY session, encodes native key input, application cursor-key mode, application keypad mode, focus reports, committed platform text, strict native copy/paste shortcuts that reject Alt and mixed Control/Super modifiers, clipboard paste shortcuts including Shift+Insert and the dedicated OS Paste key, suppresses clipboard paste accounting before a PTY session exists, suppresses empty PTY input/paste/committed-text writes, bracketed paste text, default and SGR terminal mouse reports, and PTY output-ready event proxy wakeups, and writes non-empty input bytes to the retained session; `tests/cli.rs` covers `--runtime-clipboard-paste-smoke` routing clipboard text through the runtime paste path to the retained PTY session without GPU bootstrap, `--runtime-scrollback-smoke` pumping retained history through the runtime and proving Shift+PageUp/PageDown local navigation renders without PTY writes, `--runtime-alternate-screen-smoke` pumping primary/alternate/restore output stages through the runtime, `--runtime-reflow-smoke` pumping styled OSC 8 output through the runtime before resizing terminal state plus the retained PTY session, `--runtime-focus-smoke` enabling focus-report mode then verifying focus in/out bytes and counters through the retained PTY session, `--runtime-mouse-smoke` enabling SGR mouse reporting then verifying press, release, and wheel bytes plus counters through the retained PTY session, and `--runtime-response-smoke` pumping terminal status/capability queries and verifying generated response bytes written back to the retained PTY session; `cargo run -- --runtime-focus-smoke` on 2026-06-23 pumped 8 bytes, reported focus in/out, and wrote 6 PTY input bytes across 2 focus writes; `cargo run -- --runtime-mouse-smoke` on 2026-06-23 pumped 16 bytes, reported press/release/wheel, and wrote 28 PTY input bytes across 3 mouse writes; `cargo run -- --runtime-response-smoke` on 2026-06-23 pumped 21 query bytes, wrote 1 terminal response batch with 26 bytes, and left PTY input writes at 0; `cargo bench` includes `native_input_echo_render_cycle` for deterministic native key input through PTY echo, output pump, terminal parse, and CPU-side render planning, `real_pty_shell_input_echo_roundtrip` for a real `/bin/sh` PTY input/output roundtrip through the native background reader when `/bin/sh` is available, plus `runtime_protocol_input_reports` for deterministic focus, mouse, and terminal-response protocol input paths; `tests/input.rs` covers `winit` printable, named Space, arrow, Home/End, Insert/Delete, PageUp/PageDown, F1-F24, Shift+Tab, modified BackTab, Alt-modified Enter/Backspace/Escape, Alt-modified multi-character logical keys, physical numpad normal/application keypad mode including Alt-modified numpad Enter and Alt-modified application-keypad sequences, modified named-key, control-punctuation, modified-character key encoding, and reserving Super-modified keys for native shortcuts instead of PTY input; `tests/mouse.rs` covers default and SGR mouse encoding; `tests/osc_and_paste.rs` covers bracketed paste encoding; `tests/pty.rs` verifies real background reader drain and wakeup notifications |
| `bash` | Partial | `tests/pty.rs` runs a short command and scripted interactive input/output through a real PTY when `bash` is available |
| `zsh` | Partial | `tests/pty.rs` runs a short command and scripted interactive input/output through a real PTY when `zsh` is available |
| `fish` | Partial | `tests/pty.rs` runs a short command and scripted interactive input/output through a real PTY when `fish` is available |
| `nushell` | Partial | `tests/pty.rs` runs a short command and scripted interactive input/output through a real PTY when `nu` is available |
| `vim` / `nvim` | Partial | `tests/pty.rs` runs `vim --version` and `nvim --version` through a real PTY when available, runs interactive insert/write/quit workflows through a real PTY when `vim` or `nvim` are available, and proves a Vim-attached PTY consumes an SGR mouse click to select a split window when `vim` is available; broader editor workflows pending |
| `tmux` | Partial | `tests/pty.rs` runs `tmux -V` through a real PTY when available, runs an interactive tmux pane command workflow through a real PTY when `tmux` is available, and proves a tmux-attached PTY consumes an SGR mouse click to select a pane when `tmux` is available; broader live multiplexer workflows pending |
| `less` | Partial | `tests/pty.rs` runs `less --version` through a real PTY when available, runs an interactive less search/quit workflow through a real PTY when `less` is available, and verifies real PTY alternate-screen `1049` enter/exit sequences from less with local `LESS` options cleared; broader pager workflows pending |
| `top` / `htop` / `btop` | Partial | `tests/pty.rs` runs a bounded `top` snapshot and `htop`/`btop` version checks through a real PTY when available; interactive full-screen process-monitor workflows pending |
| `ssh` | Partial | `tests/pty.rs` runs `ssh -V` through a real PTY when available; remote/session workflow pending |
| `kubectl` output | Partial | `tests/pty.rs` runs `kubectl version --client=true` through a real PTY when available; large/live Kubernetes output scenarios pending |
| `cargo test` workflow | Partial | `tests/pty.rs` runs `cargo test --quiet` and a 256-line `cargo test -- --nocapture` output workflow through a real PTY against a fixture project when `cargo` is available; large real-repo test-output scenarios pending |
| Large continuous output | Partial | Criterion parser and scrollback benches include large scrollback ingestion plus repeated scrollback viewport navigation; `tests/terminal_state.rs` feeds 200 lines through terminal state and proves scrollback remains capped to the 32 newest retained rows; `tests/pty.rs` drains a 2,000-line real PTY burst through the background reader and verifies a 256-line real `cargo test -- --nocapture` output stream; `cargo bench` includes `real_pty_shell_large_output_burst` for bounded real `/bin/sh` PTY output through the native background reader when `/bin/sh` is available; `tests/cli.rs` covers `--runtime-large-output-smoke` pumping a deterministic 512-line PTY burst through `NativeTerminalRuntime`, proving runtime scrollback is capped to 128 lines without retaining `gromaq-runtime-line-000`, rendering one dirty CPU-side frame with viewport-capped rendered dirty-cell work, and verifying the last generated line is visible in the render plan, `--runtime-bounded-state-smoke` pumping four deterministic 512-line bursts while keeping runtime scrollback lines, styled cell rows, retained cells, and rendered dirty-cell batches under explicit caps, and `--runtime-continuous-output-smoke` pumping 32 small PTY batches through the runtime with 32 rendered dirty frames, viewport-capped rendered dirty-cell work, capped scrollback, and latest visible content preservation; `cargo run -- --runtime-large-output-smoke` on 2026-06-23 pumped 12288 bytes from 512 lines, reported 128 scrollback lines, rendered 1 frame, verified `gromaq-runtime-line-511` as the last visible line, and reported render p95 500000 ns through the deterministic CPU-side path; `cargo run -- --runtime-bounded-state-smoke` on 2026-06-23 pumped 51200 bytes from 2048 lines across 4 batches, retained 128 scrollback lines, rendered 4 CPU-side frames, and verified `gromaq-bounded-line-2047` as the last visible line; `cargo run -- --runtime-continuous-output-smoke` on 2026-06-23 pumped 6912 bytes from 256 lines across 32 batches, retained 64 scrollback lines, rendered 32 CPU-side frames, and verified `gromaq-continuous-line-255` as the last visible line |
| Native `wgpu` adapter/device bootstrap | Partial | `tests/native_gpu.rs`, `tests/cli.rs`, `cargo run -- --gpu-info` on 2026-06-22 reported `Apple M1 Pro` / `Metal` |
| Offscreen GPU render/readback smoke | Partial | `cargo run -- --gpu-smoke` on 2026-06-22 read back `[26, 51, 77, 255]` from a 4x4 clear |
| GPU texture upload/readback smoke | Partial | `cargo run -- --gpu-upload-smoke` on 2026-06-22 matched `16/16` uploaded bytes; `cargo bench` includes `gpu_texture_upload_readback` for live texture upload/readback measurement when a compatible GPU adapter is available |
| Glyph atlas packing/upload smoke | Partial | `tests/glyph_atlas_image.rs`, `cargo run -- --gpu-glyph-atlas-smoke` on 2026-06-22 matched `32/32` atlas bytes; `cargo bench` includes `gpu_glyph_atlas_upload_readback` for live glyph-atlas upload/readback measurement when a compatible GPU adapter is available |
| Font-backed glyph rasterization | Partial | `tests/font_rasterizer.rs` renders a real system monospace font glyph to RGBA8 from supported macOS/Linux font candidates, rejects missing visible glyphs, and packs glyphs into an atlas bitmap; `tests/rasterized_glyph_cache.rs` covers full terminal-cell text rasterization for a combining-mark cluster and fallback-font rasterization for an emoji cell; `cargo bench` includes `font_rasterizer_combining_cell` for shaped combining-mark cell rasterization |
| Renderer-plan glyph bitmap population | Partial | `tests/rasterized_glyph_cache.rs` rasterizes distinct planned glyph atlas entries once, returns cached bitmaps for repeated render plans, and packs the resulting bitmaps; `cargo bench` includes `rasterized_glyph_cache_hot_plan` for cached repeated-frame bitmap population |
| Font-backed text atlas GPU upload/readback | Partial | `tests/cli.rs`, `cargo run -- --gpu-text-atlas-smoke` on 2026-06-23 matched `2592/2592` atlas bytes for a 36x18 real-font text atlas with a fallback emoji cell; `cargo bench` includes `gpu_text_atlas_upload_readback` for live font-backed text-atlas GPU upload/readback measurement when a compatible GPU adapter is available |
| Offscreen textured GPU quad draw/readback | Partial | `tests/cli.rs`, `cargo run -- --gpu-textured-quad-smoke` on 2026-06-22 rendered a 4x4 target with first pixel `[255, 0, 0, 255]` and 16 drawn pixels; `cargo bench` includes `gpu_textured_quad_draw_readback` for live offscreen textured-quad draw/readback measurement when a compatible GPU adapter is available |
| Offscreen terminal text GPU draw/readback | Partial | `tests/cli.rs`, `cargo run -- --gpu-terminal-text-smoke` on 2026-06-23 drew 3 terminal-planned glyph quads with 708 non-transparent output pixels from a fallback emoji text smoke; `cargo bench` includes `gpu_terminal_text_draw_readback` for live offscreen terminal glyph draw/readback measurement when a compatible GPU adapter is available |
| `wgpu` surface configuration planning | Partial | `tests/surface_config.rs` selects sRGB format, FIFO mode, opaque alpha, render-attachment usage, and rejects invalid capabilities |
| `wgpu` surface lifecycle and configuration execution | Partial | `tests/surface_config.rs` covers config planning, configure/reconfigure/zero-size lifecycle state, and applying configure/reconfigure actions to a surface backend; `tests/app.rs` covers app-owned native window surface state across initial configure, resize, zero-size suspension, clear-frame presentation delegation, and glyph-frame presentation delegation; live window surface creation pending |
| Deterministic 144Hz frame scheduler | Partial | `tests/frame_scheduler.rs` covers bounded target FPS validation plus deterministic 144Hz frame decisions; `tests/cli.rs` covers `--frame-scheduler-smoke` reporting the deterministic 144Hz target interval, frame-paced wait, presented-frame count, and dropped-frame count without GPU bootstrap; `cargo bench` includes `frame_scheduler_144hz_timeline` for deterministic 144Hz pacing decisions, late-frame/drop accounting, and idle suppression; `cargo run -- --frame-scheduler-smoke` on 2026-06-23 reported a 6944444 ns target interval, 4944444 ns frame-paced wait, 3 presented frames, and 2 dropped frames |
| Deterministic glyph atlas cache | Partial | `tests/glyph_atlas.rs` covers cache identity, LRU eviction, font-affecting style separation, and reuse across color plus box-decoration style changes; `cargo bench` includes `glyph_atlas_cache_churn` for hot lookup plus bounded LRU churn, miss, eviction, and generation reuse measurement; `tests/cli.rs` covers `--runtime-glyph-frame-smoke` reporting renderer glyph-atlas hits, misses, and entries through a runtime-rendered frame without GPU bootstrap; `cargo run -- --runtime-glyph-frame-smoke` on 2026-06-23 reported 4 renderer atlas hits, 12 misses, and 12 live entries without evictions |
| Dirty-region render planning | Partial | `tests/render_plan.rs`, `tests/wgpu_renderer.rs`; render plans retain full multi-codepoint cell text for simple ZWJ clusters, allocate distinct atlas entries for distinct multi-scalar cell text, and skip whitespace-only glyph draw commands while preserving dirty clear regions |
| Terminal glyph quad generation | Partial | `tests/glyph_quads.rs` builds pixel-space quads and atlas UVs, including wide glyphs, from render-plan output; `cargo bench` includes `glyph_quad_generation_large_plan` for CPU-side planned-glyph to vertex/index conversion |
| End-to-end mouse workflows | Partial | `tests/app.rs` covers native runtime alternate-screen SGR mouse press, release, drag, wheel, and any-motion reporting to the retained PTY session; `tests/cli.rs` covers `--runtime-mouse-smoke` routing SGR mouse press, release, and wheel reports through `NativeTerminalRuntime` into retained PTY input without GPU bootstrap; `tests/pty.rs` covers a real tmux-attached PTY consuming an SGR mouse click to select a pane when `tmux` is available and a real Vim-attached PTY consuming an SGR mouse click to select a split window when `vim` is available; broader alternate-screen app mouse workflows pending |
| Hardware-backed frame pacing | Not proven | Native surface clear-present loop is wired, but 144Hz frame pacing has not been measured on live hardware |
| Windowed terminal glyph drawing | Partial | Native redraw events render dirty terminal snapshots through `WgpuRenderer` planning, default native monospace-plus-fallback font discovery creates an app-owned rasterized glyph cache that can rasterize emoji through fallback, prepared surface glyph frames can be built from render plans and rasterized glyph bitmaps, `tests/cli.rs` covers `--runtime-glyph-frame-smoke` pumping runtime output, planning a dirty terminal frame, recording renderer glyph-atlas cache metrics, rasterizing default-native-font glyphs, and preparing an owned surface glyph frame without GPU bootstrap, `cargo run -- --runtime-glyph-frame-smoke` on 2026-06-23 pumped 19 bytes, planned 16 glyphs, reported 4 renderer atlas hits and 12 misses, rasterized 12 glyphs with 4 cache reuses, prepared 16 quads, packed 4224 atlas bytes, and reported a 256x44 frame, `cargo bench` includes `prepared_surface_glyph_frame_large_plan` for CPU-side atlas packing plus glyph-quad frame preparation, `NativeTerminalApp` wires dirty redraws to glyph-frame presentation, and `WgpuSurfaceBackend` can acquire, draw supplied terminal glyph atlas/quad batches, submit, and present a configured surface frame; live desktop screenshot/runtime proof pending |
| Full scrollback resize reflow | Partial | Styled scrollback rows rewrap on resize, and `tests/reflow.rs` preserves exact-width hard line breaks, exposes scrollback hard-break flags and logical-line IDs through `ScrollbackSnapshot`, preserves hard-newline boundaries after partial display/line erases, drops hard-newline boundaries after full line erase, merges exact-width soft-wrapped rows under the same logical-line ID, keeps soft-wrap metadata across repeated scrollback resizes, and narrows retained wide-cell rows to single-cell spans when reflowing to one column; `tests/cli.rs` covers `--runtime-reflow-smoke` pumping styled OSC 8 output through `NativeTerminalRuntime`, resizing from 10x2 to 5x2, preserving reflowed scrollback text plus hard-break flags, logical-line IDs, hyperlink and underline-color metadata, notifying the retained PTY session, and rendering the resized dirty frame without GPU bootstrap; `cargo run -- --runtime-reflow-smoke` on 2026-06-23 pumped 80 bytes, reported 1 resize event, preserved 2 scrollback lines, rendered visible lines `klmno|pqrst`, and rendered 1 CPU-side frame; broader live resize stress proof pending |
| Native OS clipboard integration | Partial | `NativeClipboard` provides a text backend through `arboard`; `cargo run -- --clipboard-smoke` on 2026-06-23 round-tripped 22 bytes through the native OS text clipboard; native app maps Control+V/Super+V and the dedicated OS Paste key to terminal paste routing and syncs terminal-owned clipboard text through the host clipboard abstraction; `tests/cli.rs` covers `--runtime-clipboard-paste-smoke` recognizing the dedicated Paste key and routing clipboard text through the retained runtime PTY session without GPU bootstrap; live desktop paste-menu smoke pending |
| OSC 52 clipboard writes | Partial | Valid OSC 52 clipboard payloads are decoded into terminal clipboard state and synchronized through the host clipboard abstraction; `tests/cli.rs` covers `--osc52-clipboard-smoke` success and readback mismatch paths; `cargo run -- --osc52-clipboard-smoke` on 2026-06-23 decoded 18 bytes, verified native OS clipboard readback, and restored previous readable text |
