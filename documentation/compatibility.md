# Compatibility Matrix

This matrix records what the repository currently proves through deterministic
tests, real PTY workflows, scripted interaction, and live smoke commands. It is
not a claim of full daily-driver compatibility yet; it is a proof map for the
remaining terminal-core work.

For `0.2.1`, Gromaq is a public alpha/beta terminal foundation release. The
matrix below documents installable early-adopter coverage and proof gaps; it
does not claim v1 stability, full daily-driver completeness, accepted live
desktop visual inspection/screenshot proof, Developer ID signed/notarized
macOS app distribution, 144 Hz hardware acceptance, or broad host compatibility.
Developer ID signed/notarized macOS app distribution remains a known proof gap
for this release.

On 2026-06-28, `cargo test --test pty -- --nocapture` passed all 44 PTY tests
on the current macOS host. The captured output confirmed live current-host
coverage for bash, zsh, vim, tmux, less, top, ssh, kubectl, and the tiny cargo
fixture workflows, and also confirmed that fish, nushell, nvim, htop, and btop
were skipped because those binaries were not on PATH.
The same run, `cargo run -- --runtime-tool-workflow-smoke` passed both
current-host tool checks with 0 skips: `ssh -V` produced 31 bytes containing
`OpenSSH`, and `kubectl version --client=true --output=yaml` produced output
containing `clientVersion`. A refreshed local run after adding safe
`ssh -G localhost` and `kubectl config view --output=jsonpath={.kind}`
workflows passed all 4 runtime tool checks with 0 skips: `ssh-version`
produced 31 bytes containing `OpenSSH`, `ssh-config` produced 4226 bytes
containing `hostname localhost`, `kubectl-version` produced 276 bytes
containing `clientVersion`, and `kubectl-config` produced 6 bytes containing
`Config`. A later refreshed local run after adding safe `vim --version`,
`tmux -V`, and `less --version` workflows passed all 7 runtime tool checks
with 0 skips: `vim-version` produced 2912 bytes containing `VIM`,
`tmux-version` produced 11 bytes containing `tmux`, `less-version` produced
296 bytes containing `less`, and the existing `ssh` and `kubectl` checks kept
passing with the same expected markers. A later refreshed local run after
adding safe `fish --version`, `nu -c` command output, `nvim --version`, `top`
snapshot, `htop --version`, `btop --version`, and quiet plus large-output Cargo
fixture workflows checked 15 runtime tool workflows, passed 10 present tools, skipped
`fish-version`, `nu-command`, `nvim-version`, `htop-version`, and
`btop-version` because those binaries were not on PATH, passed `top-snapshot`
with output containing `Processes`, passed `cargo-test` with output containing
`test result: ok`, passed `cargo-large-output` with output containing
`gromaq-cargo-output-255`, and recorded 0 failures.
`scripts/prove-current-host-compatibility.sh` passed on 2026-06-28 UTC,
writing the host inventory, PTY test log, and runtime external-tool smoke log
under `target/compatibility-proof`. A refreshed local run under
`target/compatibility-cargo-large-proof` passed all 44 PTY tests, recorded
`tools_present=8`, `tools_missing=5`, `pty_tests_passed=44`, and runtime smoke
summary counts of `runtime_tool_workflow_checked=15`,
`runtime_tool_workflow_passed=10`, `runtime_tool_workflow_skipped=5`, and
`runtime_tool_workflow_failed=0`, plus passed/skipped workflow name lists.
Use `scripts/prove-current-host-compatibility.sh` to refresh this proof as a
single bundle. The helper records the current host's tool inventory and writes
the `cargo test --test pty -- --nocapture` and
`cargo run -- --runtime-tool-workflow-smoke` logs under
`target/compatibility-proof` along with a compact `summary.txt` success marker
that includes host uname/OS/architecture, Rust and Cargo versions, git commit
and dirty-worktree state, present/missing tool inventory counts, PTY tests
passed, and runtime tool workflow checked, passed, skipped, and failed counts
plus passed/skipped workflow names.
`.github/workflows/ci.yml` is configured to run the helper in the macOS `rust`
job and upload `target/compatibility-proof/*` as the
`gromaq-current-host-compatibility-proof` artifact. The workflow is also
configured with a Linux compatibility job that installs common Ubuntu
shell/editor/TUI tools, runs the same helper, and uploads
`gromaq-linux-compatibility-proof`; that job sets
`GROMAQ_REQUIRED_COMPAT_TOOLS` so expected installed tools fail closed instead
of becoming silent skips. On 2026-06-28 UTC, CI run `28314822034` passed the
macOS current-host compatibility helper and the Linux compatibility job and
uploaded their proof artifacts before the overall run failed later in the macOS
welcome-preview threshold step.
After a successful pushed CI run exists for a commit,
`scripts/prove-ci-compatibility-artifacts.sh` can download the macOS and Linux
compatibility artifacts for that exact commit, verify `host_os=Darwin` and
`host_os=Linux`, require `runtime_tool_workflow_failed=0` in both summaries,
and write `target/ci-compatibility-proof/summary.txt`.

## Shells

| Workflow | Current proof | Status |
| --- | --- | --- |
| `/bin/sh` interactive input/output | Native PTY smoke, real-shell command-output smoke, and app runtime tests | Proven in CI/local tests |
| `bash` command lifecycle | Real PTY command and interactive workflows when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed the current-host bash command and interactive checks. A refreshed focused run passed `pty_session_runs_bash_cwd_workflow_when_available`, proving the interactive bash PTY starts in the repository working directory. | Proven on current host for command, interactive output, and CWD lifecycle; conditional elsewhere |
| `zsh` command lifecycle and repaint preservation | Real PTY command workflow plus native redraw preservation test and `cargo run -- --runtime-repaint-smoke` deterministic zsh-style prompt repaint proof. On 2026-06-27, `cargo test --test pty -- --nocapture` passed the current-host zsh command and interactive checks. A refreshed focused run passed `pty_session_runs_zsh_cwd_workflow_when_available`, proving an interactive `zsh -f` PTY starts in the repository working directory without host startup-file prompts. | Proven on current host for command, interactive output, no-rc CWD lifecycle, and deterministic repaint proof |
| `fish` command lifecycle | Real PTY command and interactive workflows when available. On 2026-06-27, the current host did not have `fish` on PATH, so this workflow remains unproven locally. `cargo run -- --runtime-tool-workflow-smoke` now includes a `fish-version` check that passes when `fish --version` contains `fish` and reports a skip when the binary is absent; the current host skip keeps this unproven locally. | Conditional on host binary; not proven on current host |
| `nushell` command lifecycle | Real PTY command and interactive workflows when available. On 2026-06-27, the current host did not have `nu` on PATH, so this workflow remains unproven locally. `cargo run -- --runtime-tool-workflow-smoke` now includes a `nu-command` check that runs `nu -c` with deterministic output when the binary is present and reports a skip when it is absent; the current host skip keeps this unproven locally. | Conditional on host binary; not proven on current host |

## Editors, Pagers, and Multiplexers

| Workflow | Current proof | Status |
| --- | --- | --- |
| `vim` launch and navigation workflows | Real PTY command workflow when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed current-host `vim --version` and scripted edit workflows. A refreshed focused run passed `pty_session_runs_vim_search_navigation_when_available`, proving slash-search navigation to `gromaq-vim-line-040` and command-mode result capture through the PTY. A refreshed `cargo run -- --runtime-tool-workflow-smoke` also passed `vim-version` with 2912 output bytes containing `VIM`. | Proven on current host for launch, edit, search navigation, and runtime version smoke; conditional elsewhere |
| `vim` alternate-screen enter/exit | Scripted real PTY workflow when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed current-host Vim alternate-screen enter/exit proof. | Proven on current host; conditional elsewhere |
| `vim` SGR mouse split selection | Scripted real PTY workflow when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed current-host Vim SGR mouse split-window selection proof. | Proven on current host; conditional elsewhere |
| `nvim` launch workflow | Real PTY command workflow when available. On 2026-06-27, the current host did not have `nvim` on PATH, so this workflow remains unproven locally. `cargo run -- --runtime-tool-workflow-smoke` now includes an `nvim-version` check that passes when `nvim --version` contains `NVIM` and reports a skip when the binary is absent; the current host skip keeps this unproven locally. | Conditional on host binary; not proven on current host |
| `nvim` alternate-screen enter/exit | Scripted real PTY workflow when available. On 2026-06-27, the current host did not have `nvim` on PATH, so this workflow remains unproven locally. | Conditional on host binary; not proven on current host |
| `nvim` SGR mouse split selection | Scripted real PTY workflow when available. On 2026-06-27, the current host did not have `nvim` on PATH, so this workflow remains unproven locally. | Conditional on host binary; not proven on current host |
| `tmux` launch and prefix-key workflows | Real PTY command and interactive pane workflows when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed current-host `tmux -V` and interactive pane checks. A refreshed focused run passed `pty_session_runs_tmux_prefix_split_pane_when_available`, proving `Ctrl-b %` split-pane command routing through the PTY into an isolated tmux server. A refreshed `cargo run -- --runtime-tool-workflow-smoke` also passed `tmux-version` with 11 output bytes containing `tmux`. | Proven on current host for launch, interactive shell, prefix split-pane workflows, and runtime version smoke; conditional elsewhere |
| `tmux` SGR mouse pane selection | Scripted real PTY workflow when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed current-host tmux SGR mouse pane-selection proof. | Proven on current host; conditional elsewhere |
| Native tmux assist catalog and manager UI | `cargo run -- --tmux-assist` on 2026-06-30 reported tmux installed as `tmux 3.5a`, outside tmux, no attachable sessions, and listed the native action registry with each action id, tmux command, keybinding hint when known, destructive classification, confirmation requirement, active-tmux requirement, and outside-tmux availability. Runtime rendering tests prove Control/Super Shift `T` opens a renderer-safe native tmux manager panel without mutating the terminal grid, renders the live target pane title/command/dimensions in the manager header when available, and keeps pane row title/command/dimension labels visible. App and CLI config tests prove the no-argument native app opens that manager on startup when tmux UI is enabled, `[tmux] open_manager_on_start = false` keeps tmux UI enabled without opening the startup panel, and `[tmux] show_status_strip = false` hides only the persistent strip while the retained manager panel still renders. App tests prove Esc close, arrow/vim navigation, destructive confirmation/cancel, safe action dispatch through the tmux action runner, inline name entry for start-session and rename-session/window, terminal-PTY attach handoff, explicit refresh shortcut handling, post-action snapshot refresh, missing/no-server/outside-tmux hint rows that keep Enter, `r` refresh, `?` help, and Esc visible while action rows retain tmux commands, mouse clicks select session/window/pane/action/workspace rows, and closed-panel shell input pass-through. | Proven locally as a native CLI assist surface plus in-window manager panel model/render/input path; live app-window interaction proof remains separate |
| Native tmux action command | `gromaq --tmux-action <id> [target] [name] [--confirm]` uses the same action registry and action runner as the smoke path. Tests prove stable action-id lookup, destructive confirmation gating before execution, unknown-action rejection, and no-active-session results for current-session actions without a target. | Proven in unit and CLI tests for command construction, confirmation, and no-active-session handling; live user-triggered safe action execution remains conditional on being inside tmux or providing a valid target |
| Native tmux manager report | `gromaq --tmux-manager` reports tmux installation status, session/window/pane counts, current target when a tmux client context exists, current-session windows, current-window panes, and the registry-backed manager actions users can run through `gromaq --tmux-action`. Tests prove current-scope filtering and clean no-server/no-tmux output. | Proven locally as a native read-only manager/report and action discovery surface |
| Native tmux action/state manager smoke | `cargo run -- --runtime-tmux-smoke` on 2026-06-30 used an isolated tmux socket, created session `gromaq-runtime-tmux`, ran split-pane and new-window actions through the native action runner, read state through stable `list-sessions`, `list-windows`, and `list-panes` formats, observed 1 session, 2 windows, and 3 panes, then killed the isolated server. The command reports a clean skip if tmux is not on PATH. `--runtime-tmux-smoke` and `--runtime-tmux-ui-smoke` fail closed when required proof lines are false or required tmux state counts are zero. | Proven locally when tmux is installed; conditional elsewhere |
| Native tmux UI smoke | `cargo run -- --runtime-tmux-ui-smoke` on 2026-07-01 used an isolated tmux socket, opened the native manager panel model, rendered the persistent tmux status strip and manager panel through the runtime renderer boundary, checked the destructive confirmation path, reported `startup manager after shell prompt checked: true`, `status pane command checked: true`, `target pane detail checked: true`, `current pane marker checked: true`, `cancellation feedback checked: true`, `destructive shortcut checked: true`, `unavailable shortcut blocked: true`, `mouse focus checked: true`, `mouse action selection checked: true`, `mouse workspace selection checked: true`, `refresh shortcut requested: true`, `refresh focus preserved: true`, `help catalog checked: true`, `new window shortcut checked: true`, `window cycle shortcuts checked: true`, `zoom shortcut checked: true`, `zoom toggle shortcut checked: true`, `attach session pty handoff checked: true`, `detach session failure feedback checked: true`, `tmux missing feedback checked: true`, `rename window action dispatched: true`, `rename window feedback checked: true`, `rename session action dispatched: true`, `rename session feedback checked: true`, `split right shortcut checked: true`, `split down shortcut checked: true`, `kill pane confirmation dispatched: true`, `kill window confirmation dispatched: true`, `kill session confirmation dispatched: true`, `start session pty handoff checked: true`, `start session feedback checked: true`, `workspace feedback checked: true`, `workspace command hints checked: true`, `workspace existing attach checked: true`, `workspace failure feedback checked: true`, and `workspace invalid preflight checked: true`, dispatched shortcut actions for new-window, next-window, previous-window, zoom-pane toggle, select-pane, rename-window, rename-session, split-pane-right, split-pane-down, confirmed kill-pane, confirmed kill-window, confirmed kill-session against a disposable selected session, proved start-session, rename-window, and rename-session success feedback visibly render in the manager panel, proved the help catalog renders tmux commands and key hints from the manager, proved detach-session command failure feedback visibly renders when no client can be detached, proved tmux-missing action feedback visibly renders in the manager panel, proved the current pane stays visibly marked when the manager selection moves, handed attach-session through the retained PTY, dispatched start-session through inline name entry and handed its attach command through the retained PTY, started a workspace preset, proved workspace command hints render, proved an existing workspace session attaches through the retained PTY, proved the second workspace launch did not duplicate the session, proved workspace command-runner failure feedback and invalid-workspace preflight feedback visibly render in the manager panel, read tmux state, and killed the isolated server. The command reports a clean skip if tmux is not on PATH and fails closed on incomplete required proof output. | Proven locally when tmux is installed for the automated manager/render/action/name-entry/workspace path; manual app-window workflow proof remains separate |
| Native tmux manager window snapshot | `cargo run -- --window-tmux-manager-snapshot target/native-tmux-manual-proof/window-tmux-manager-snapshot.ppm` launches a bounded native window through the default welcome/default shell startup path with the tmux manager pre-opened, reports `default startup content checked: true`, `tmux status pane command rendered: true`, `tmux manager sessions:`, `tmux manager windows:`, and `tmux manager panes:`, and writes a prepared glyph-frame PPM. `scripts/prove-native-tmux-default-snapshot.sh` wraps the same command, creates an isolated tmux session with a split pane, fails closed unless the tmux status strip, active pane command, manager panel, and manager state-count lines render into the frame report, writes `target/native-tmux-default-snapshot-proof/summary.txt`, and exports an inspectable PNG when `sips` is available. | Proven as native window glyph-frame snapshot output for the no-argument startup content path with real tmux session/window/pane state and active pane command visibility; still separate from live keyboard/mouse app-window proof |
| Native tmux default cargo-run manual proof | `scripts/prove-macos-native-tmux-default-cargo-run.sh` prepares uniquely named tmux sessions, launches plain `cargo run` with no app config, records git branch/dirty metadata plus the full `git-status.txt`, records the exact `target/debug/gromaq` string marker in `tmux-default-cargo-run-binary-markers.txt`, fails if the old keyboard/mouse/paste startup copy remains in that binary, preflights the bounded default window smoke for current startup copy, status strip rendering, pane-command coverage reporting, and manager rendering, writes `tmux-default-cargo-run-manager-reference.ppm` plus a PNG when `sips` is available from `cargo run -- --window-tmux-manager-snapshot`, fails unless that reference reports current startup content, status strip, active pane command text, manager rendering, and manager session/window/pane count lines, asks the tester to close the startup-open manager with Esc before proving Control/Super Shift `T` reopens it, records manual confirmation files under `target/macos-native-tmux-default-cargo-run-proof` for the current startup shortcut copy, default status strip, Control/Super Shift `T` manager panel, non-hint UI, visible sessions/windows/panes/current target/pane command text, keyboard navigation, start-session, attach-session, safe action dispatch, new-window, destructive confirmation, isolated kill target, shell input after closing the panel, and right-prompt legibility, then reads tmux directly and fails unless the named started session exists, the target session has at least 2 windows, the target session has at least 3 panes, and the disposable kill-session target is absent. | Harness present for exact no-argument developer launch proof with stale-binary, smoke-preflight, checkout-state, startup-open manager reopen, reference snapshot artifact, and post-action tmux-state guards; it still requires the tester to run and complete the live app-window checklist |
| Native tmux manual app-window proof harness | `scripts/prove-macos-native-tmux-manual.sh` prepares uniquely named tmux sessions, opens either the selected macOS app bundle or a direct debug binary (`target/debug/gromaq` by default when no app bundle is present) with `gromaq-native-tmux-manual.toml`, writes `launch-mode.txt`, `open-manager-on-start.txt`, and `git-status.txt`, records git branch/dirty metadata in the summary, defaults to `open_manager_on_start = false` so the tester must open the real manager with Control/Super Shift `T`, enables the persistent tmux status strip, records separate `tmux-status-strip-visible.txt`, `tmux-manager-visible.txt`, `tmux-navigation-checked.txt`, `tmux-right-prompt-legible.txt`, `tmux-start-session.txt`, `tmux-attach-session.txt`, `tmux-safe-action.txt`, `tmux-new-window.txt`, `tmux-refresh-checked.txt`, and `tmux-workspace-visible.txt` confirmations, exposes a workspace preset, asks the tester to confirm destructive confirmation against the disposable session, workspace visibility, and normal shell input after closing the panel, then reads tmux directly and fails unless the named started session exists, the target session has at least 2 windows, the target session has at least 3 panes, and the disposable kill-session target is absent before writing `target/macos-native-tmux-manual-proof/summary.txt`. `GROMAQ_MANUAL_TMUX_OPEN_ON_START=true` keeps startup-open proof available. | Harness present for current-session manual proof through app-bundle or debug-binary launch with checkout-state and post-action tmux-state metadata; it does not replace actually running the live app-window checklist |
| tmux workspace presets | Config parsing and validation support `[tmux]` and `[tmux.workspaces.<name>]` presets with session, optional root, windows, and pane commands. Unit tests prove validation rejects empty pane commands, the native app config carries enabled presets into the manager panel, keyboard focus reaches the Workspaces row, Enter requests launch, and runtime dispatch uses the existing launcher for start-or-attach behavior without duplicating an existing session. | Proven in unit/app/CLI-smoke tests for parser, validation, configured native UI exposure, keyboard launch, launcher command construction, and isolated live start/no-duplicate proof; live non-headless attach remains terminal-dependent |
| `less` launch and paging workflows | Real PTY command, interactive search, and paging-navigation workflows when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed current-host `less --version` and search checks. A refreshed focused run passed `pty_session_runs_less_paging_navigation_when_available`, proving `G` paging navigation to the generated final line through the PTY. A refreshed `cargo run -- --runtime-tool-workflow-smoke` also passed `less-version` with 296 output bytes containing `less`. | Proven on current host for launch, search, paging navigation, and runtime version smoke; conditional elsewhere |
| `less` alternate-screen enter/exit | Scripted real PTY workflow when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed current-host less alternate-screen enter/exit proof. | Proven on current host; conditional elsewhere |

## CLI and TUI Programs

| Workflow | Current proof | Status |
| --- | --- | --- |
| `top` launch workflow | Real PTY command workflow when available. On 2026-06-27, `cargo test --test pty -- --nocapture` passed the current-host `top` snapshot check. A refreshed `cargo run -- --runtime-tool-workflow-smoke` also passed `top-snapshot` with output containing `Processes` on macOS. | Proven on current host for PTY snapshot and runtime snapshot smoke; conditional elsewhere |
| `htop`, `btop` launch workflows | Real PTY command workflows when available. On 2026-06-27, the current host did not have `htop` or `btop` on PATH, so these workflows remain unproven locally. `cargo run -- --runtime-tool-workflow-smoke` now includes `htop-version` and `btop-version` checks that pass when their `--version` output contains the matching tool name and report skips when the binaries are absent; the current host skips keep these unproven locally. | Conditional on host binaries; not proven on current host |
| `ssh` launch workflow | Real PTY command workflow when available plus `cargo run -- --runtime-tool-workflow-smoke`, which runs `ssh -V` in a native PTY and requires `OpenSSH` output when the binary is present. It also runs the safe non-network `ssh -G localhost` config-dump workflow and requires `hostname localhost` output. On 2026-06-27, `cargo test --test pty -- --nocapture` passed the current-host `ssh -V` workflow, and a refreshed local focused PTY run passed `pty_session_runs_ssh_config_dump_when_available`. A refreshed `cargo run -- --runtime-tool-workflow-smoke` passed `ssh-version` with 31 output bytes and `ssh-config` with 4226 output bytes. | Proven for current-host client/version and non-network config-dump workflows; smoke reports pass or skip elsewhere |
| `kubectl` output workflow | Real PTY command workflow when available plus `cargo run -- --runtime-tool-workflow-smoke`, which runs `kubectl version --client=true --output=yaml` in a native PTY and requires `clientVersion` output when the binary is present. It also runs the safe local `kubectl config view --output=jsonpath={.kind}` workflow and requires `Config` output without contacting a cluster. On 2026-06-28, `cargo test --test pty -- --nocapture` passed the current-host `kubectl version --client=true --output=yaml` workflow, and a refreshed local focused PTY run passed `pty_session_runs_kubectl_config_view_when_available`. A refreshed `cargo run -- --runtime-tool-workflow-smoke` passed `kubectl-version` with 276 output bytes and `kubectl-config` with 6 output bytes. | Proven for current-host client/version and non-network local config-view workflows; smoke reports pass or skip elsewhere |
| `cargo test -- --nocapture` output | Real PTY fixture workflow with deterministic large output. On 2026-06-27, `cargo test --test pty -- --nocapture` passed current-host quiet and large-output cargo fixture checks. A refreshed `cargo run -- --runtime-tool-workflow-smoke` also passed the quiet `cargo-test` fixture with output containing `test result: ok` and `cargo-large-output` with output containing `gromaq-cargo-output-255`. | Proven on current host |

## Terminal Features

| Feature | Current proof | Status |
| --- | --- | --- |
| Alternate-screen enter/leave restoration | Golden state tests, runtime alternate-screen smoke, and focused tests for primary grid, cursor, rendition, charset, scrollback, repeated enter, and resize restoration | Proven |
| Resize/reflow | Golden reflow tests cover visible-grid and scrollback reflow, hard-break boundaries, style metadata, hyperlinks, underline colors, wide and emoji clusters, and a wide linked underlined cell; runtime reflow smoke covers current shell-output resize plumbing | Proven for covered cases |
| ANSI/SGR styling | Parser, state, and fixture tests cover SGR colors/styles, DEC special graphics through both G0 and G1 shift-out/shift-in paths, saved-cursor charset restoration, and golden VT editing fixtures | Proven for covered sequences |
| Unicode wide/combining/emoji clusters | Terminal-state tests and glyph rasterization tests | Proven for covered clusters |
| Scrollback retention and viewport navigation | Unit/integration tests and runtime scrollback smoke | Proven |
| Selection/copy and OSC 52 clipboard | Unit tests cover visible-grid copy, displayed scrollback copy, displayed styled scrollback with wide-cell text, soft-wrap versus hard-break newlines, emoji clusters, wide-cell trailing-half selection, viewport clamping, stale-selection clearing, and native clipboard smoke paths. `cargo run -- --runtime-selection-copy-smoke` verifies deterministic runtime PTY output can be visibly selected across rows including a wide cell, copied into the injected host clipboard, recognizes dedicated Copy, Control+Insert, Control+Shift+C, and Super+C as native copy shortcuts, and leaves PTY input writes at zero. `cargo run -- --runtime-osc52-clipboard-smoke` verifies OSC 52 emitted by runtime PTY output syncs terminal-owned clipboard text into the injected host clipboard, suppresses a repeated sync of the same terminal-owned text, restores prior host clipboard text, and leaves PTY input writes at zero. | Proven for covered paths plus deterministic runtime selection copy, native copy shortcuts, and runtime OSC 52 host sync |
| Structured test/control API | `TerminalTestApi` covers key encoding, paste, resize, visible grid, scrollback, cursor, performance counters, OSC title, OSC 52 clipboard text, terminal-generated response-byte draining, and deterministic one-pixel-per-cell screenshots | Proven for covered terminal snapshots and response bytes |
| Terminal-generated status and window reports | Terminal-state tests cover primary and secondary device attributes, status reports, DECRQSS mode/style reports, cursor-position reports, OSC title/icon-label reports, and xterm window queries for open state, window position, text-area pixel size, screen pixel size via `CSI 15 t`, and character dimensions. The `CSI 15 t` screen-pixel response uses the configured terminal pixel height/width and returns `CSI 5 ; height ; width t`, matching the xterm report shape. OSC/window tests also cover `CSI 22;0/1/2 t` plus `CSI 23;0/1/2 t` save/restore of both the current icon label and window title, only the icon label, or only the window title. | Proven for covered terminal response bytes; live native screen geometry fidelity remains bounded by configured pixel dimensions |
| PTY background reader EOF handling | `cargo test --lib pty_reader_` proves that Unix PTY `EIO` from the background reader is treated as EOF while other read errors remain fatal; `cargo test --test pty` passed all 44 PTY workflow tests on 2026-06-28 after the fix | Proven for covered PTY reader and real-workflow paths |
| Keyboard input modes | Unit/integration tests for common, application cursor, keypad, focus, paste, committed text, and native shortcuts, including bracketed and plain runtime paste, dedicated Paste, Shift+Insert, Control+V, and Super+V paste routing. On 2026-06-27, `cargo run -- --runtime-clipboard-paste-smoke` recognized `dedicated-paste`, `shift-insert`, `control-v`, and `super-v`, wrote 30 clipboard bytes to the PTY, recorded 1 clipboard paste, and restored the previous clipboard text. `cargo run -- --runtime-bracketed-paste-smoke` verifies that runtime PTY output enabling bracketed paste causes multiline UTF-8 paste text to be wrapped with `CSI 200~` / `CSI 201~` before PTY writeback while preserving payload byte accounting. `cargo run -- --runtime-committed-text-smoke` verifies that committed Unicode text writes its UTF-8 payload to the PTY through the committed-text path without counting as native key input or paste. | Proven for covered keys, deterministic clipboard paste routing, bracketed paste writeback, and committed UTF-8 text input |
| Browser-style terminal text zoom | Native shortcut mapping tests plus native app renderer/grid reconfiguration tests for increase, decrease, reset, shifted plus, modifier-wheel, and dedicated OS/browser zoom-key policy; `cargo run -- --runtime-text-zoom-smoke` verifies default 32/18/44 px metrics zoom to 37/21/51 px, reduces the visible grid, and resets without a live GPU window | Proven for covered controls |
| Mouse reporting modes | Runtime mouse smoke now covers SGR press, release, drag, any-motion, wheel report writeback, default-protocol press/release writeback, and X10 press-only writeback through the PTY input path; unit and terminal-state tests cover default protocol reporting plus X10 DECRQM state reports; app-runtime tests cover direct SGR, default-protocol, and X10 send-mouse-input writeback, and alternate-screen mouse tests cover runtime app paths | Proven for default, X10, and SGR covered paths |
| Shell prompt repaint output retention | App redraw tests and `cargo run -- --runtime-repaint-smoke` prove zsh-style prompt repaint sequences preserve the command line, two output rows, and repainted prompt in a full-viewport render plan after the swapchain-clear boundary | Proven for covered repaint sequence |
| Theme color propagation | Renderer config mapping plus prepared-frame tests for background, ANSI foreground, selection, and cursor colors | Proven for covered paths |
| Built-in theme legibility | Config contrast tests for foreground, cursor, selection, and readable ANSI slots across shipped presets, `cargo run -- --theme-legibility-smoke` CLI proof for the shipped default `gromaq-ghostty` preset and default text metrics, `cargo run -- --theme-preview-snapshot <path>` PPM artifact proof for default text, ANSI colors, selection, cursor, and padding with high-contrast text-pixel plus exact selection/cursor pixel gates, `cargo run -- --theme-preview-config <config> <path>` proof for TOML theme overrides plus background, cursor, and selection opacity through the same prepared-frame path, plus prepared-frame preview pixel tests for default padding, foreground glyph coverage, cursor color, straight-alpha opacity, and unclipped cell edges. `scripts/prove-theme-preview.sh` wraps the default and configured preview paths, checks visual metric floors and configured opacity values, writes proof artifacts under `target/theme-preview-proof`, including a compact `summary.txt` plus `metrics.txt` with default/configured frame, opacity, text-pixel, selection, cursor, prepared-quad, and atlas metrics, and CI is configured to upload that directory as `gromaq-theme-preview-proof` with `if: always()`. On 2026-06-28 UTC, CI run `28316513803` passed `--theme-legibility-smoke`, passed `scripts/prove-theme-preview.sh`, and printed the theme metrics artifact lines, including default/configured frame `1036x292`, high-contrast text pixels `9416` / `9519`, selection pixels `11150`, prepared quads `118`, and atlas bytes `126720`. | Proven for shipped presets, default prepared-frame path, covered TOML theme override path, helper-backed remote artifact upload, theme summary artifact, and theme metrics artifact |
| Default and configured terminal font stack | Native font resolver tests for polished user fonts, including JetBrains Mono Nerd Font, MesloLGS Nerd Font, Geist Mono, Monaspace Neon, Fira Code, and Hack, before system fallbacks; `[font].fallback_families` parsing, config-check reporting, launch wiring, and reload tests cover ordered user fallback names or explicit font paths before automatic symbol and emoji fallbacks. The automatic fallback candidates include standard symbol fonts such as Apple Symbols and Noto Sans Symbols before emoji fonts, so non-emoji decorative glyphs can rasterize through the same fallback cache path. `cargo run -- --font-symbol-fallback-smoke` plans and rasterizes a Braille sample through the default native glyph cache without GPU bootstrap; on 2026-06-28, the local proof planned 1 glyph, rasterized 1 glyph, wrote a 10x22 bitmap, and counted 126 alpha pixels. | Proven for covered primary and fallback paths plus local default-stack Braille fallback rasterization |
| Default welcome preview artifact | `cargo run -- --welcome-preview-snapshot <path>` and CLI tests render the sectioned welcome screen to a deterministic PPM through the prepared glyph-frame path without GPU bootstrap, while checking high-contrast text pixels, avatar color pixels, glyph/cursor/atlas metrics, the committed avatar row count, and argument handling. `scripts/prove-welcome-preview.sh` wraps the same path, enforces fail-closed visual floors for the reported text, avatar, and glyph metrics, validates the binary PPM header, requires the committed ANSI avatar to remain 17 rows, and writes proof artifacts under `target/welcome-preview-proof`, including a compact `summary.txt` plus `metrics.txt` with the exact frame size, terminal cells, avatar rows, text pixels, avatar pixels, glyph quads, cursor quads, and atlas bytes used by the proof; `.github/workflows/ci.yml` syntax-checks and runs the helper in the macOS `rust` job, then uploads `target/welcome-preview-proof/*` as the `gromaq-welcome-preview-proof` artifact. The workflow is also configured to run `scripts/prove-readme-welcome-preview.sh` and upload `target/readme-welcome-preview-proof/*` under the same artifact so the committed README screenshot freshness proof is retained. That upload uses `if: always()` so partial logs and generated images are retained when a visual proof command fails after writing diagnostics or when a later macOS CI step fails. On 2026-06-28, the refreshed local 36x17-sampled Braille-avatar proof wrote a 1468x820 PPM with 25564 high-contrast text pixels, 36299 avatar color pixels, 692 glyph quads, 0 cursor quads, 624624 atlas bytes, and `Metric: avatar rows=17`. Earlier CI runs proved the previous block-avatar path: run `28315944025` rendered 22858 high-contrast text pixels and 126062 avatar color pixels on macOS 26, run `28316513803` proved the bounded-delta README freshness helper, and run `28321082609` completed green for commit `84740e3` after rerunning avatar freshness, default welcome proof, and README proof helpers for that previous 33x17 block avatar. CI run `28326188288` completed green for commit `0dfed64` after rerunning avatar freshness, default welcome proof, and README proof helpers for the previous 33x17 Braille avatar; the macOS proof accepted 20509 high-contrast text pixels, 25966 avatar color pixels, 654 glyph quads, 0 cursor quads, 576576 atlas bytes, and `Metric: avatar rows=17`. | Proven locally and remotely for the deterministic prepared-frame Braille-avatar artifact path, README screenshot freshness, welcome metrics artifact, avatar-row summary artifact, and helper-backed CI artifact upload |
| GPU welcome image snapshot | `cargo run -- --welcome-image-snapshot target/gromaq-welcome-image-current.ppm` on 2026-06-27 rendered the embedded `images/avatar/avatar-splash.rgba` through the native `wgpu` textured image-quad path into a 480x480 PPM with the default `#101216` background pixel, center image pixel `[207, 193, 209, 255]`, 691215 bytes written, and 50423 non-background drawn pixels. CLI tests cover artifact writing, missing path errors, and extra-argument rejection. GitHub Actions CI run 28299568944 completed green on 2026-06-27 for commit `12f7dfe`, including the same `--welcome-image-snapshot target/gromaq-welcome-image-ci.ppm` command on macOS. | Proven locally on Metal and remotely on GitHub Actions macOS |
| README welcome preview image | `cargo run -- --welcome-preview-snapshot target/gromaq-welcome-preview-readme.ppm` on 2026-06-28 produced a 1468x820 prepared-frame PPM with 25564 high-contrast text pixels, 36299 avatar color pixels, 692 glyph quads, 0 cursor quads, and 624624 atlas bytes; the generated PNG was copied to `images/screenshots/gromaq-welcome-preview.png` for the README. `scripts/prove-readme-welcome-preview.sh` reruns the preview proof, compares decoded RGB pixels from the generated PPM against the committed README PNG, requires matching dimensions, and allows bounded host font-rasterization variance up to 150000 changed pixels and mean absolute RGB-channel delta 8.0. The local refreshed README proof reported matching 1468x820 dimensions, 22 changed pixels, and mean delta 0.00 against the committed PNG. CI run `28326188288` completed green for commit `0dfed64` after rerunning the helper in the macOS `rust` job for the previous 33x17 Braille avatar. The helper writes a compact `summary.txt` artifact after the pixel comparison passes. The glyph/text counts reflect the aspect-correct 36x17-sampled Braille welcome avatar and shift if the avatar or welcome copy is regenerated. | Proven locally and remotely as committed prepared-frame visual asset with bounded decoded-pixel freshness proof |
| Welcome avatar aspect-correct rendering | `images/avatar/generate.mjs` renders the welcome avatar through the shared `images/tools/gromaq-image-assets.mjs` asset tool, which derives terminal sample dimensions from a `terminalCellAspect` of 18/44 (the default gromaq cell) so each sampled region matches the on-screen cell instead of the prior fixed 4:8 ratio. The avatar is sampled at 36x17 Braille cells with 2x4 subcell dot masks, preserving finer terminal detail than the earlier quadrant-block avatar while still fitting the default welcome layout; trailing blank normalization leaves the committed `images/avatar/avatar-welcome.ansi` at a uniform 35-cell visible width across 17 rows, contains at least 300 Braille glyph cells, and keeps every welcome stat, including the 24-character tagline, unclipped in the 69-column default window (1280px / 18px cell - 28px padding) by relying on the stat column's own leading spacing instead of a separate avatar/stat gap. `src/app/welcome/tests.rs` guards the 17-row line count, uniform width, supported Braille glyph set, dense foreground/color detail, width-per-row ratio >= 1.9, at least 300 Braille glyph cells, and unclipped narrow-window tagline so the aspect correction and subcell detail cannot silently regress. `node images/avatar/generate.mjs --check` regenerates the avatar PNG/RGBA/ANSI outputs in memory and fails if committed assets, including `avatar-welcome.ansi`, are stale; the refreshed local check passed after regeneration, and CI run `28326188288` reran the same freshness check successfully on macOS for the previous 33x17 avatar. | Proven locally and remotely for committed ANSI avatar geometry, Braille subcell detail, and generator freshness |
| Welcome avatar contrast floor | `boostTerminalColor` in `images/tools/gromaq-image-assets.mjs` applies a WCAG luminance floor against the default `#101216` background after the hue-shaping boost, snapping to the final 8-bit channels and adding neutral light until each cell reaches the 3.0:1 graphical contrast minimum. Without it the dark source avatar rendered at min 1.13:1 / mean 2.14:1 with 83.4% of cells below 3:1 (nearly invisible); the committed avatar now measures min 3.0:1 / mean 3.57:1 with 100% of cells at or above 3:1 (checking the rounded output values so the floor survives float-to-u8 rounding), resolving the muddy low-contrast output against the dark background. | Proven for the deterministic prepared-frame welcome avatar path |
| Frame FPS status overlay | Runtime rendering tests prove the FPS/status text is added only to a cloned render snapshot, appears in the rendered frame when its target cells are blank, appends an overlay dirty span, skips occupied right-prompt cells, and does not mutate terminal-owned grid text or shell scrollback | Proven for covered runtime render path |

## Performance and Runtime Bounds

| Workflow | Current proof | Status |
| --- | --- | --- |
| Deterministic idle CPU path | `cargo run -- --runtime-idle-cpu-smoke` on 2026-06-27 sampled the clean-frame idle path 5 times at 50 ms intervals, reported max process CPU 4.0% against the 5.0% budget, performed 16 render attempts, skipped all 16 clean frames, and rendered 0 frames. CI runs the same smoke command. | Proven for deterministic clean-frame runtime idle path; live-window near-zero idle CPU still requires separate measurement |
| Bounded long-session memory growth | `cargo run -- --runtime-memory-smoke` on 2026-06-27 pumped 110592 bytes from 4608 lines, rendered 9 frames, retained the configured 128-line scrollback cap, kept retained scrollback cells at 3054 against the 4096-cell deterministic cap, reported RSS growth 1344 KiB against the 65536 KiB cap, and observed `gromaq-memory-line-4607`. CI runs the same smoke command. | Proven for deterministic runtime bounded-state and RSS-growth gate; live-window long interactive memory stability still requires separate measurement |
| Real-shell large output render budget | `cargo run -- --runtime-real-shell-large-output-smoke` on 2026-06-27 drove `/bin/sh` through a 256-line burst, pumped 7168 bytes, retained the configured 64-line scrollback cap, evicted the first line, observed the last line, rendered 2 dirty frames, and reported render p95 1000000 ns against the 6940000 ns budget. CI runs the same smoke command. | Proven for current-host real-shell large-output render budget and bounded scrollback behavior; broader live TUI/output stress remains ongoing compatibility work |
| Deterministic input-to-render latency budget | `cargo run -- --runtime-perf-budget-smoke` on 2026-06-28 reported `runtime perf budget smoke: ok` across 20 deterministic input/render samples, pumped 20 bytes, rendered 20 frames, reported render p95 100000 ns against `render p95 budget ns: 6940000`, and reported `input-to-render p95 ns: 500000` against `input-to-render p95 budget ns: 10000000`. CI runs the same smoke command. | Proven for deterministic runtime input-to-render budget; live-window latency still requires separate measurement |
| Repeated runtime p95 budget | `cargo run -- --runtime-perf-p95-smoke` on 2026-06-28 reported `runtime perf p95 smoke: ok` across 20 deterministic input/render samples, pumped 20 bytes, rendered 20 frames, reported render p95 100000 ns against `render p95 budget ns: 6940000`, and reported `input-to-render p95 ns: 100000` against `input-to-render p95 budget ns: 10000000`. CI runs the same smoke command. | Proven for repeated deterministic runtime p95 samples; live-window p95 still requires separate measurement |

## Live Native Window Proof

| Workflow | Current proof | Status |
| --- | --- | --- |
| Native window startup | `cargo run -- --window-smoke` on 2026-06-27 presented one native surface frame after 4 redraw attempts with 0 timeouts and 3 occluded acquisitions | Proven in current live run |
| Native glyph-frame presentation | `cargo run -- --window-perf-smoke` on 2026-06-25 presented 192 live glyph frames with a 2548x1568 glyph frame, 74 glyph quads, 73920 atlas bytes, and 17 occupied atlas slots. A 2026-06-27 retry still reached the glyph-frame path with 86 glyph quads, 1 cursor quad, and a 2548x1568 frame, but it did not refresh frame-pacing proof because live desktop conditions produced dropped frames or heavy surface occlusion. | Proven for live glyph-frame presentation; current frame-pacing proof not refreshed |
| Prepared native glyph-frame preview artifact | `--runtime-glyph-frame-snapshot <path>` writes a deterministic PPM from the same owned glyph-frame preparation path used before surface presentation | Proven as CPU-side preview, not a live desktop screenshot |
| Native-window glyph-frame snapshot artifact | `cargo run -- --window-glyph-frame-snapshot target/gromaq-window-glyph-frame-current.ppm` on 2026-06-25 wrote a 2548x1568 PPM with 11985809 bytes, 60 glyph quads, and 1 cursor quad from the prepared native-window frame path | Proven as native presentation-path artifact, not an OS compositor screenshot |
| Generated logo window icon | Native app config embeds `images/logos/logo-icon-128.rgba` and sets it on `winit` `WindowAttributes`; app config tests assert the icon is present before native window creation | Proven for the cross-platform `winit` window-icon boundary |
| Linux desktop identity assets | `packaging/linux/dev.gromaq.Gromaq.desktop` uses `Icon=dev.gromaq.Gromaq`, AppStream metainfo declares the same desktop id, and `scripts/install.sh` installs both plus the generated hicolor icon under user-local XDG data paths by default. On 2026-06-25, `GROMAQ_SKIP_CARGO_INSTALL=1 GROMAQ_PLATFORM=Linux GROMAQ_ASSET_ROOT="$PWD" GROMAQ_INSTALL_ROOT=target/install-proof sh scripts/install.sh` placed `share/applications/dev.gromaq.Gromaq.desktop`, `share/icons/hicolor/256x256/apps/dev.gromaq.Gromaq.png`, and `share/metainfo/dev.gromaq.Gromaq.metainfo.xml` under `target/install-proof`. | Proven for non-network install-root placement; live desktop-menu refresh not yet proven |
| macOS app bundle identity assets | `GROMAQ_BINARY_PATH=target/debug/gromaq scripts/package-macos-app.sh` on 2026-06-29 created `target/dist/Gromaq.app`, ad-hoc signed it, and passed `codesign --verify --deep --strict --verbose=4`. `file target/dist/Gromaq.app/Contents/MacOS/gromaq` reported an arm64 Mach-O for the local debug binary. `GROMAQ_MACOS_UNIVERSAL=1 scripts/package-macos-app.sh` built both macOS targets, combined them with `lipo -create`, ad-hoc signed the app, and passed strict codesign verification; `file target/dist/Gromaq.app/Contents/MacOS/gromaq` reported a Mach-O universal binary with x86_64 and arm64 slices, and `lipo -info` reported `x86_64 arm64`. `codesign -dv --verbose=4 target/dist/Gromaq.app` reported `Signature=adhoc` and `Format=app bundle with Mach-O universal (x86_64 arm64)`. `tests/packaging.rs::macos_app_script_builds_well_formed_bundle_from_stub_binary` and `tests/packaging/macos_signing.rs::macos_app_script_ad_hoc_signs_and_verifies_by_default` re-prove bundle metadata, icon metadata, default ad-hoc signing, and strict verification with stub binaries on macOS. | Proven locally for bundle generation, icon metadata, default ad-hoc signing, strict codesign verification, and universal app binary assembly; live Dock/Finder UI workflow remains separate |
| macOS packaged app executable launch | `target/dist/Gromaq.app/Contents/MacOS/gromaq --window-smoke` on 2026-06-27 launched the packaged executable from the `.app` bundle and presented one native surface frame after 4 redraw attempts with 0 timeouts and 3 occluded acquisitions. On 2026-06-27, `open -W -n -o target/macos-open-proof.stdout --stderr target/macos-open-proof.stderr target/dist/Gromaq.app --args --window-smoke` returned 0 through LaunchServices and captured `window smoke: ok`, `presented frame limit: 1`, `redraw attempts: 4`, `surface timeouts: 0`, and `surface occluded: 3`; stderr contained only macOS's secure restorable-state warning. On 2026-06-28, `scripts/prove-macos-app-identity.sh` packaged `target/debug/gromaq`, launched the packaged app through LaunchServices with `--window-screenshot-smoke`, kept it alive long enough for live OS identity checks, and now writes `target/macos-app-identity-proof/summary.txt` after all checks pass: System Events returned `gromaq` for bundle identifier `dev.gromaq.Gromaq`, `lsappinfo` reported `CFBundleIdentifier=dev.gromaq.Gromaq` and `LSDisplayName=Gromaq`, `pgrep` observed the bundled `Contents/MacOS/gromaq --window-screenshot-smoke` process, and the smoke completed with 900 presented frames, 910 redraw attempts, and 10 occluded acquisitions. A later current-host rerun launched the packaged app and found the bundle/process identity, but did not refresh the summary because the smoke reported 3600 redraw attempts, 0 timeouts, and 3600 fully occluded surface acquisitions; a direct `cargo run -- --window-screenshot-smoke` reproduced the same all-occluded surface state. | Proven locally for packaged executable launch, LaunchServices `open` launch, and historical live bundle-id/display-name registration while the packaged app is running; the refreshed summary artifact is currently blocked by all-occluded macOS surface acquisition, and manual Dock/Finder UI workflow is not yet proven |
| macOS ad-hoc app signing | `scripts/package-macos-app.sh` now ad-hoc signs by default through `GROMAQ_CODESIGN_IDENTITY=-` and always runs `codesign --verify --deep --strict --verbose=4`. Set `GROMAQ_CODESIGN_IDENTITY` to a Developer ID Application identity to use hardened runtime and timestamp options instead. On 2026-06-29, the default local debug package and the universal package both passed strict codesign verification; `spctl -a -vvv -t execute target/dist/Gromaq.app` rejected the app because the local proof is ad-hoc signed and not notarized. | Proven locally and by macOS packaging tests for ad-hoc signing and strict verification; Developer ID signing and notarization not yet proven |
| macOS notarization helper | `scripts/notarize-macos-app.sh` prepares a notary zip with `ditto`, supports `GROMAQ_NOTARY_KEYCHAIN_PROFILE` or Apple ID/password/team credentials for `xcrun notarytool submit --wait`, staples with `xcrun stapler`, validates the staple, and runs `spctl` assessment when available. On 2026-06-27, `GROMAQ_NOTARY_DRY_RUN=1 scripts/notarize-macos-app.sh target/dist/Gromaq.app` created `target/dist/Gromaq-notary.zip` and printed the planned submit/staple/validate steps without requiring Apple credentials. `tests/packaging.rs::notarization_script_dry_run_prepares_archive_without_credentials` now re-proves the dry-run archive and planned submit/staple/validate output on macOS. CI syntax-checks the helper. | Helper and dry-run archive path proven; live notarization requires Apple credentials |
| macOS one-command app bundle install | `CARGO_HOME="$PWD/target/macos-install-open-proof/cargo" GROMAQ_SKIP_CARGO_INSTALL=1 GROMAQ_PLATFORM=Darwin GROMAQ_INSTALL_APP_BUNDLE=1 GROMAQ_RAW_BASE="file://$PWD" GROMAQ_MACOS_APP_DIR="$PWD/target/macos-install-open-proof/apps" sh scripts/install.sh` on 2026-06-27 prepared packaging assets without `GROMAQ_ASSET_ROOT`, copied `Gromaq.app` into the configured app directory, and `plutil -lint target/macos-install-open-proof/apps/Gromaq.app/Contents/Info.plist` passed. `open -W -n -o target/macos-install-open-proof/open.stdout --stderr target/macos-install-open-proof/open.stderr target/macos-install-open-proof/apps/Gromaq.app --args --window-smoke` returned 0 through LaunchServices and captured `window smoke: ok`, `presented frame limit: 1`, `redraw attempts: 4`, `surface timeouts: 0`, and `surface occluded: 3`; stderr contained only macOS's secure restorable-state warning. | Proven locally with a file-backed raw asset base, supplied installed binary, installed app copy, and LaunchServices smoke launch; manual Dock/Finder UI workflow not yet proven |
| Linux release tarball artifact | `GROMAQ_BINARY_PATH=target/debug/gromaq GROMAQ_RELEASE_TARGET=darwin-arm64-debug-proof scripts/package-linux-tarball.sh` on 2026-06-25 produced `target/dist/gromaq-0.1.0-darwin-arm64-debug-proof.tar.gz`; `LC_ALL=C tar -tzf` confirmed the archive contains `bin/gromaq`, `README.md`, `LICENSE`, the desktop file, AppStream metainfo, and the hicolor icon. `tests/packaging.rs::linux_tarball_script_assembles_complete_archive_from_stub_binary` (Unix-guarded) runs the script with a stub binary so CI re-proves archive assembly on every Unix host: it asserts `bin/gromaq`, `README.md`, `LICENSE`, the desktop file, AppStream metainfo, and the hicolor icon entries in the tarball listing, plus `gromaq-linux-tarball-summary.txt` in the dist directory. Release workflow run 28303243197 for commit `12a38e8` uploaded `gromaq-0.1.0-linux-x86_64.tar.gz` from the Ubuntu Linux artifact set; downloaded inspection confirmed the tarball payload and the Linux `SHA256SUMS` listed the tarball checksum (`b600ea4706569567e15b9ec548a3ea27d825b80eae5de805cfdc8b78c3ce3a4a`). | Proven for archive assembly and local summary writing in CI on Unix hosts with a stub binary, and remotely as an uploaded Linux release artifact with checksum coverage before the summary artifact was added |
| Release installer paths | `scripts/install.sh` supports `GROMAQ_INSTALL_METHOD=release` for Linux tarball assets and macOS app bundle assets. Linux downloads `gromaq-<version>-linux-<arch>.tar.gz`, verifies `SHA256SUMS-linux-<arch>`, installs the binary, and copies desktop assets from the tarball. macOS downloads `Gromaq-macos-app.zip`, verifies `SHA256SUMS-macos-app`, extracts `Gromaq.app`, copies it to `${GROMAQ_MACOS_APP_DIR:-~/Applications}`, and verifies that `Contents/MacOS/gromaq --version` reports the requested version. `tests/install_dry_run.rs` proves Linux and macOS release dry-runs without writes and ensures the macOS release dry-run does not mention Cargo. `tests/install_release.rs` proves Linux checksum mismatch, missing checksum entries, custom checksum names, checksum opt-out, unsupported platform rejection, macOS missing checksum entry rejection, and macOS zip install from a local release base. | Proven locally without network or home writes for Linux and macOS release install paths; live published `v0.2.1` release install proof remains pending until the release exists |
| Installer dry-run audit path | `GROMAQ_DRY_RUN=1 GROMAQ_PLATFORM=Linux GROMAQ_INSTALL_ROOT=target/install-dry-run-proof GROMAQ_ASSET_ROOT="$PWD" sh scripts/install.sh` prints the planned source install command, expected binary path, and Linux desktop asset destination, then exits with `Dry run complete; no files written.` `tests/install_dry_run.rs::install_script_dry_run_reports_actions_without_writing_install_root` re-proves this behavior. `GROMAQ_DRY_RUN=1 GROMAQ_PLATFORM=Darwin GROMAQ_INSTALL_APP_BUNDLE=1 GROMAQ_MACOS_APP_DIR=... sh scripts/install.sh` proves source app-bundle planning, while `GROMAQ_DRY_RUN=1 GROMAQ_PLATFORM=Darwin GROMAQ_INSTALL_METHOD=release GROMAQ_VERSION=v0.2.1 sh scripts/install.sh` proves the macOS release app plan without Cargo wording. | Proven for Linux and macOS source/release dry-run planning without writes |
| Linux desktop database refresh hook | `scripts/install.sh` refreshes the Linux desktop database for the installed `share/applications` directory when `update-desktop-database` is available, reports the refreshed directory on success, and treats refresh failures as non-fatal install warnings. `tests/install_dry_run.rs::install_script_refreshes_linux_desktop_database_when_available` injects a fake `update-desktop-database` on `PATH`, runs the Linux install-root path with `GROMAQ_SKIP_CARGO_INSTALL=1`, and asserts both the reported refresh path and the command argument written by the fake utility. `.github/workflows/ci.yml` installs `desktop-file-utils` before the Ubuntu Linux install-root proof so the same installer path has the real utility on `PATH`; CI run `28309262840` completed green for commit `461006d` after running that Linux install-root proof in the `linux-packaging` job. | Proven locally for deterministic source-install refresh invocation and remotely in Ubuntu CI with `desktop-file-utils` installed; live desktop menu UI discovery remains unproven |
| Linux desktop metadata/cache discovery helper | `scripts/prove-linux-desktop-discovery.sh` is a Linux-only helper for desktop hosts with `desktop-file-validate`, `appstreamcli`, `update-desktop-database`, and `gtk-update-icon-cache`. It installs the desktop identity payloads into `target/linux-desktop-discovery-proof` by default with `GROMAQ_SKIP_CARGO_INSTALL=1`, validates `dev.gromaq.Gromaq.desktop`, validates `dev.gromaq.Gromaq.metainfo.xml`, verifies the hicolor icon payload, refreshes the proof-root desktop database and icon cache, writes a compact `summary.txt`, and prints that it does not prove live menu UI rendering. CI syntax-checks the helper, the Ubuntu `linux-packaging` job installs the required metadata tools and runs it, and repository policy guards the required validation markers. CI run `28315342470` passed this helper in the `linux-packaging` job. | Helper present for Linux maintainers and proven in Ubuntu CI for metadata/cache validation including the summary artifact; this is still not a live menu UI proof |
| Debian package artifact | `tests/packaging.rs::debian_package_script_assembles_installable_desktop_package_from_stub_binary` runs `scripts/package-debian-deb.sh` with a stub binary and `GROMAQ_DEB_ARCH=amd64`, then verifies the `.deb` ar members are `debian-binary`, `control.tar.gz`, and `data.tar.gz`, validates `Package`, `Version`, `Architecture`, `Maintainer`, and `Description` control metadata, confirms Debian `postinst`/`postrm` desktop metadata refresh hooks are present in `control.tar.gz`, and confirms the data payload contains `/usr/bin/gromaq`, README, copyright, desktop file, AppStream metainfo, and hicolor icon under `/usr`. The script writes the Debian ar container directly instead of relying on platform-specific `ar` creation flags, and it builds the package without requiring `dpkg-deb`, so it is testable on normal Unix hosts. CI run `28309262840` completed green for commit `461006d`, including Debian package install and payload checks through `sudo dpkg -i target/dist/gromaq_*.deb`, `/usr/bin/gromaq --version`, and `dpkg -L gromaq` before the release-tarball installer proof. `scripts/prove-debian-package.sh` now writes `target/dist/gromaq-debian-proof-summary.txt` with the installed payload entry count after package install and payload checks pass. Release workflow run 28303243197 uploaded `gromaq_0.1.0_amd64.deb`; downloaded artifact inspection confirmed canonical members and the Linux `SHA256SUMS` listed the package checksum (`c4e2126cc2f5f1d72f60e28dfaf9009466c4eda7911b564e934f04937f081afd`). | Proven locally with stub binary and maintainer-script inspection, remotely in CI for Debian package install and payload checks before the summary artifact was added, and remotely as an uploaded release artifact with checksum coverage before the maintainer hooks and full package-install CI proof were added; the Debian proof summary payload count awaits the next live Debian proof run |
| Arch package recipe | `packaging/arch/PKGBUILD` defines a `gromaq-git` Arch source package that builds the public Git checkout with `cargo build --release --locked` and installs `/usr/bin/gromaq`, README documentation, the MIT license, `packaging/linux/dev.gromaq.Gromaq.desktop`, `packaging/linux/dev.gromaq.Gromaq.metainfo.xml`, and `images/logos/logo-icon-256.png` into Arch package paths. `packaging/arch/.SRCINFO` records the package metadata for AUR-style publication, and `packaging/arch/gromaq.install` provides Arch `gromaq.install` desktop metadata refresh hooks for package install, upgrade, and removal. CI runs `bash -n packaging/arch/PKGBUILD`, and repository policy checks the expected Cargo build, desktop identity payload markers, `.SRCINFO` source metadata, and hook markers. `scripts/prove-arch-package.sh` mirrors the full Arch build/install proof in an `archlinux:base-devel` container when Docker is available and writes `target/arch-package-proof-summary.txt` with the installed payload entry count after success. On 2026-06-27, GitHub Actions CI run 28301610408 first proved the remote `arch-packaging` job for commit `c4feef2`; later run 28303175039 for commit `12a38e8` re-proved the same metadata-oriented job under `archlinux:base-devel`: it installed `git` and `rust`, checked `.SRCINFO`, switched to an unprivileged builder user, and ran `makepkg --nobuild --noconfirm` plus `makepkg --printsrcinfo` from `packaging/arch`. On 2026-06-28 UTC, CI runs 28308158338, `28309262840`, `28314822034`, `28315342470`, and `28315944025` passed full `arch-packaging` jobs with `makepkg --noconfirm`, `pacman -U --noconfirm`, `test -x /usr/bin/gromaq`, `/usr/bin/gromaq --version`, `pacman -Ql gromaq-git`, and `makepkg --printsrcinfo`; runs `28314822034`, `28315342470`, and `28315944025` were still overall red because later macOS jobs failed. CI run `28321082609` completed green for commit `84740e3` with the same full Arch build, install, executable, payload-listing, and `.SRCINFO` checks. Release workflow run 28303243197 uploaded `packaging/arch/PKGBUILD` and hidden `packaging/arch/.SRCINFO` in the Linux artifact set with checksum coverage before `gromaq.install` was added. | Syntax, `.SRCINFO`, payload markers, Arch hook marker policy, Arch CI `makepkg --nobuild` / `.SRCINFO` generation proof, local proof helper, and repeated full remote Arch package build/install proof with `gromaq.install` are proven; the local Arch helper payload-count summary awaits the next local Docker proof run, and release workflow artifact upload for `gromaq.install` still needs a new release-workflow run |
| Release artifact workflow | `.github/workflows/release.yml` builds Linux and macOS release artifacts on tag/manual runs, uploads `target/dist/*.tar.gz`, uploads `target/dist/*.deb`, `packaging/arch/PKGBUILD`, `packaging/arch/.SRCINFO`, `packaging/arch/gromaq.install`, and `target/dist/Gromaq-macos-app.zip` as workflow artifacts, and on tag-triggered runs uses `gh release create` plus `gh release upload` to publish the tarball, Debian package, Arch `PKGBUILD`, `.SRCINFO`, `gromaq.install`, macOS `.app` zip, and distinct `SHA256SUMS-linux-x86_64` / `SHA256SUMS-macos-app` manifests to the matching GitHub Release. GitHub stores the hidden `.SRCINFO` upload as the release asset `default.SRCINFO`, so the live proof helpers verify that asset name while the source package metadata remains `packaging/arch/.SRCINFO`. CI checks shell syntax for the packaging scripts and project policy checks the workflow markers, including the tag-only release upload path. `scripts/prove-github-release-publication.sh` verifies a published release is non-draft, non-prerelease, has the expected tag, and contains the Linux tarball, Debian package, Arch metadata files, macOS app zip, and platform checksum manifests before writing `target/github-release-publication-proof/summary.txt`. On 2026-06-27, release workflow run 28303243197 completed green for commit `12a38e8`: the Linux workflow artifact downloaded with `target/dist/gromaq-0.1.0-linux-x86_64.tar.gz`, `target/dist/gromaq_0.1.0_amd64.deb`, `target/dist/SHA256SUMS`, `packaging/arch/PKGBUILD`, and the hidden `packaging/arch/.SRCINFO`; the macOS workflow artifact downloaded with `Gromaq-macos-app.zip` and `SHA256SUMS`, and the zipped `Gromaq.app/Contents/Info.plist` contains `LSApplicationCategoryType = public.app-category.utilities`. A live `v0.1.0` publication proof on 2026-06-28 verified the published, non-draft, non-prerelease release asset set: Linux tarball, Debian package, Arch `PKGBUILD`, `default.SRCINFO`, `gromaq.install`, macOS app zip, and both checksum manifests. | Proven remotely for workflow artifact upload at run 28303243197 / commit `12a38e8`, including hidden `.SRCINFO`, Linux tarball, Debian package, checksums, and macOS app zip before `gromaq.install` was added; live GitHub Release publication is proven for `v0.1.0` with GitHub's `default.SRCINFO` hidden-file asset name |
| Release checksum manifest | `scripts/generate-checksums.sh` writes `target/dist/SHA256SUMS` for `.tar.gz`, `.deb`, and `.zip` artifacts, accepts `GROMAQ_CHECKSUM_MANIFEST` for platform-specific manifest paths, accepts `GROMAQ_CHECKSUM_EXTRA_FILES` for non-archive release assets such as `packaging/arch/PKGBUILD`, `packaging/arch/.SRCINFO`, and `packaging/arch/gromaq.install`, and reports the manifest entry count after writing; release jobs upload the manifest with each artifact set and CI syntax-checks the script. `tests/checksums.rs::checksum_script_can_include_additional_release_assets` proves a local manifest can include `PKGBUILD`, `.SRCINFO`, and `gromaq.install` alongside a release tarball and reports `Checksum entries: 4`, `tests/checksums.rs::checksum_script_writes_custom_manifest_path` proves custom manifest output, `tests/checksums.rs::checksum_script_rejects_missing_additional_release_assets` proves missing extra files fail without leaving a manifest behind, and `tests/checksums.rs::checksum_script_rejects_empty_release_directory_without_manifest` proves empty artifact directories fail without writing a manifest. The Linux packaging/release jobs use `GROMAQ_CHECKSUM_EXTRA_FILES="packaging/arch/PKGBUILD packaging/arch/.SRCINFO packaging/arch/gromaq.install"`. CI run `28316513803` proved the Linux packaging checksum entry-count output remotely with `Checksum entries: 5` from the release-install helper. Release workflow run 28303243197 uploaded a Linux `SHA256SUMS` containing `.SRCINFO` (`21200b8a4bc3f1142986c3998e7107ecc6bce1b3b1f27fd83e864e1cf9c91a48`), `PKGBUILD` (`4f916fa22e18bcef0257d26b6e1220a37dea0fee5fbe90c78797fb5c38b62de9`), `gromaq-0.1.0-linux-x86_64.tar.gz` (`b600ea4706569567e15b9ec548a3ea27d825b80eae5de805cfdc8b78c3ce3a4a`), and `gromaq_0.1.0_amd64.deb` (`c4e2126cc2f5f1d72f60e28dfaf9009466c4eda7911b564e934f04937f081afd`); the macOS artifact uploaded `SHA256SUMS` containing `Gromaq-macos-app.zip` (`f67782ad0905fae288df7714ab6f28adf3e28187edf4f3787799979e1a83f6ff`). | Proven locally and in remote Linux packaging CI for Linux tarball, Debian package, Arch metadata plus hook, manifest generation, and entry-count output; old release workflow artifact proof predates the Arch hook |
| Linux packaging CI job | `.github/workflows/ci.yml` includes a focused `linux-packaging` job on `ubuntu-latest` that syntax-checks install/package scripts, installs `desktop-file-utils`, runs `cargo test --test project_policy`, runs Linux install-root desktop asset proof with `GROMAQ_SKIP_CARGO_INSTALL=1`, builds the release tarball, builds the Debian package, runs `scripts/prove-linux-release-install.sh` with Arch metadata checksum extras so the helper packages the local release tarball, writes `SHA256SUMS-linux-x86_64`, installs with `GROMAQ_INSTALL_METHOD=release`, and checks the installed binary plus desktop identity payloads under `target/release-install-proof`, including a compact `summary.txt`. | CI run `28321082609` proved the helper-backed Linux packaging path remotely on Ubuntu for commit `84740e3`: Debian package install, `/usr/bin/gromaq --version`, `dpkg -L gromaq` payload checks, tarball packaging, `GROMAQ_CHECKSUM_EXTRA_FILES="packaging/arch/PKGBUILD packaging/arch/.SRCINFO packaging/arch/gromaq.install" scripts/prove-linux-release-install.sh`, and `test -x target/release-install-proof/bin/gromaq` all passed. Earlier runs `28309262840`, `28314822034`, `28315342470`, `28315944025`, and `28316513803` also proved the helper-backed packaging path. |
| GPU offscreen pipeline CI coverage | `.github/workflows/ci.yml` runs the offscreen GPU smokes (`--gpu-smoke`, `--gpu-upload-smoke`, `--gpu-textured-quad-smoke`, `--gpu-terminal-text-smoke`, `--gpu-glyph-atlas-smoke`, `--gpu-text-atlas-smoke`) and the `--frame-scheduler-smoke` (which proves the deterministic 144Hz target interval of 6944444 ns and frame-paced wait without a live display) in the macOS `rust` job, extending CI from the existing `WgpuRenderer` device-init/planning tests in `cargo test --all` to the GPU adapter bootstrap, offscreen render, pixel readback, and 144Hz frame-scheduler timeline paths. The GPU smokes exit non-zero if no adapter is available; this is safe on the macOS runner because it exposes Metal and the `WgpuRenderer` tests already require and pass on it. All six GPU smokes and the frame-scheduler smoke pass locally on Apple M1 Pro / Metal and passed in the green remote `rust` job in CI run 28299568944 on 2026-06-27. | Proven locally on Metal and remotely on GitHub Actions macos-latest |
| Remote GitHub Actions CI pipeline | On 2026-06-28 UTC, push `3474653` triggered `.github/workflows/ci.yml` run `28310371344`, which completed green. The macOS `rust` job ran `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --all`, every theme/runtime/real-shell perf smoke, the welcome preview proof helper, the welcome image snapshot, the offscreen GPU smokes, the `--frame-scheduler-smoke`, and `cargo bench --bench parser_throughput -- --list`, then uploaded `gromaq-welcome-preview-proof`. Later CI run `28315944025` for commit `ee8be2f` completed overall red only because the macOS `rust` job failed in `scripts/prove-readme-welcome-preview.sh` after exact decoded-pixel comparison rejected macOS 26 font-rasterization drift. In that run, `linux-packaging`, `arch-packaging`, and `linux-compatibility` completed green, and the macOS `rust` job passed syntax checks, fmt, `git diff --check`, clippy, `cargo test --all`, theme legibility, theme preview proof, avatar freshness, current-host compatibility, and the default welcome-preview proof before the README freshness failure. The downloaded failed-run artifact had matching 1468x820 dimensions, 102801 changed pixels, and mean delta 6.77 against the committed README PNG, which fits the later bounded-delta proof. CI run `28316513803` for commit `0976263` completed green: `linux-packaging`, `rust`, `arch-packaging`, and `linux-compatibility` all succeeded, including the bounded README welcome freshness helper, welcome metrics artifact, README freshness summary artifact, checksum entry-count output, Debian package install/payload checks, and full Arch package build/install/payload checks. CI run `28321082609` for commit `84740e3` completed green with all four jobs succeeding; the macOS `rust` job reran avatar freshness, welcome/readme proof helpers, runtime/theme/GPU smokes, and parser benchmark listing, while Linux packaging, Linux compatibility, and Arch packaging all passed. CI run `28326188288` completed green for commit `0dfed64` with all four jobs succeeding after rerunning the current Braille-avatar freshness check, welcome/readme proof helpers, runtime/theme/GPU smokes, parser benchmark listing, Linux packaging, Linux compatibility, and full Arch package build/install checks. This does not prove live desktop behavior. The separate `release.yml` artifact-upload path is proven by run 28303243197 for workflow artifacts containing the Linux tarball, Debian package, Arch `PKGBUILD`, hidden `.SRCINFO`, macOS app zip, and checksum manifests; the live `v0.1.0` GitHub Release publication proof verifies the published release asset set separately. | Green end-to-end ci.yml proof is proven through run `28326188288` for commit `0dfed64`; live desktop behavior and live Linux release install proof remain separate proof gaps |
| macOS live-window screenshot proof utility | `scripts/capture-macos-window-proof.sh` preflights macOS Screen Recording permission with `CGPreflightScreenCaptureAccess()` before building or launching the app, builds the debug binary before launching `target/debug/gromaq --window-screenshot-smoke`, starts window lookup after the short `GROMAQ_SCREENSHOT_DELAY_SECONDS` default, locates a visible `Gromaq` window through `CGWindowListCopyWindowInfo`, reads `CGWindowBounds`, `kCGWindowSharingState`, layer, and alpha, captures that specific window with `screencapture -x -l <window-id>`, falls back to bounded `screencapture -x -R<x,y,w,h>` only when the window-id capture fails, preserves separate stderr logs for the targeted window and region fallback captures, validates the resulting PNG with `NSBitmapImageRep` by requiring sampled pixels near the default `#101216` terminal background and default `#eef4fb` foreground text, writes `target/gromaq-live-window-proof-summary.txt` after accepted validation, deletes rejected PNGs, and waits for the app process to finish. The native startup path carries an explicit `screen_capture_allowed` policy and applies `set_content_protected(!allowed)` before surface creation so the macOS window is not intentionally protected from read-only screen capture. CI syntax-checks the script and project policy checks that it uses the screenshot-specific bounded live-window host, prebuild/direct binary launch, macOS Screen Recording preflight, macOS window metadata including sharing state, targeted window capture, bounded region fallback, separate capture diagnostics, content validation, short lookup delay, app-side capture allowance, shareability diagnostics, and rejected-output cleanup instead of the frame-pacing smoke or an unvalidated full-desktop screenshot. On 2026-06-28, a current Mac run with the 900-frame screenshot host found window id 54671, region `116,67,1280,828`, sharing state `0`, layer `0`, alpha `1.0`, and the smoke reported 900 presented frames with 62 glyph quads and 1 cursor quad; the helper logged that macOS window content is not shareable, window-id capture failed with `could not create image from window`, bounded region capture returned non-terminal pixels, validation rejected it with 0 sampled default terminal-background pixels and 0 sampled default foreground pixels, and cleanup removed the rejected PNG. A later 2026-06-28 rerun failed earlier with `macOS screen capture access preflight: false`, so this terminal session still lacks the Screen Recording permission required for accepted screenshot proof. No screenshot artifact was accepted from either run. | Proof utility present and fail-closed with Screen Recording preflight, longer bounded prebuild/direct launch, app-side capture allowance, sharing-state diagnostics, blocked-shareability explanation, bounded fallback, preserved capture diagnostics, content validation, accepted-capture summary writing, and rejected-output cleanup; current host still lacks accepted live screenshot proof |
| Active-monitor frame pacing | `cargo run -- --window-perf-smoke` on 2026-06-25 collected 180 post-warmup samples on a 120000 mHz monitor, reported `frame interval target limited by monitor: true`, measured exact p95 8845125 ns against the 10000000 ns active-monitor budget, recorded 0 dropped frames, and reported `frame pacing accepted: true`. On 2026-06-27, current-host retries did not replace that proof: one run presented 192/192 frames with exact p95 9098750 ns but 2 dropped frames, and another was dominated by surface occlusion. On 2026-06-28, `scripts/prove-144hz-window-perf.sh` reached the live glyph-frame path on this Mac with a 2560x1600 window, 86 glyph quads, 1 cursor quad, 110880 atlas bytes, and 192 presented frames, but rejected the run because the active monitor reported `monitor refresh mhz: 120000`, limited the frame target to 120 fps, and recorded 2 dropped frames. | Previously proven for the current 120Hz monitor; the latest current-host attempt is useful live-window evidence but not accepted pacing proof |
| 144Hz display pacing plus latency gate | `scripts/prove-144hz-window-perf.sh` is a fail-closed manual helper for 144Hz-capable desktops. It runs `cargo run -- --window-perf-smoke`, records `target/144hz-window-perf-proof/window-perf.log`, and refuses proof unless the live window reports monitor refresh `>= 144000` mHz, `frame interval target fps: 144`, `frame interval target limited by monitor: false`, `dropped frames: 0`, and `frame pacing accepted: true`. After the live frame-pacing gate passes, it runs `cargo run -- --runtime-perf-p95-smoke`, records `target/144hz-window-perf-proof/runtime-perf-p95.log`, and requires the existing `input-to-render p95 budget ns: 10000000` marker before writing `summary.txt`. On 2026-06-28, the helper correctly rejected this Mac before the latency gate because the display reported `monitor refresh mhz: 120000`, `frame interval target fps: 120`, `frame interval target limited by monitor: true`, `dropped frames: 2`, and `frame pacing accepted: false`. | Helper present and fail-closed for live 144Hz frame pacing plus deterministic runtime latency budget; live 144Hz hardware proof requires a 144Hz-capable display and is not yet proven |
| Desktop screenshot of windowed glyph output | Requires a live screenshot artifact that visibly contains the Gromaq native window; the macOS helper now targets the detected Gromaq window id, records CoreGraphics sharing diagnostics, logs blocked shareability when `kCGWindowSharingState` is `0`, can fall back to the detected bounds, and rejects captures that do not contain enough sampled default terminal-background and foreground text pixels. The 2026-06-28 current-host attempt reached the fallback path with sharing state `0` but was rejected by validation and removed, so no PNG is accepted as proof. | Not yet proven |
| Desktop OS paste menu | The menu-equivalent `NamedKey::Paste` route is covered by shortcut-policy tests and `--runtime-clipboard-paste-smoke`, but selecting a real desktop menu item in a live native window still requires manual OS workflow proof | Not yet proven as a live OS menu workflow |
| macOS signed/notarized app distribution | The packaging script supports Developer ID signing through `GROMAQ_CODESIGN_IDENTITY`, and the notarization helper supports notarytool submission, stapling, validation, and `spctl` assessment. A Developer ID signed and Apple-notarized release still requires Apple credentials plus live notary submission, stapling, and verification proof. | Not yet proven for Developer ID signing or notarization |

## Remaining Matrix Work

- Run the current-host scripted workflows on a broader host matrix.
- Add accepted live desktop visual inspection/screenshot artifacts for the
  native window path.
- Add 144Hz hardware proof on a 144Hz-capable monitor.
- Expand editor/multiplexer interaction beyond the current scripted edit,
  Vim search navigation, tmux prefix split, less search/paging,
  alternate-screen, and mouse workflows.
- Expand `ssh` and `kubectl` beyond current version plus non-network local
  configuration workflows when safe live targets exist.

Post-v1 deferred work includes live Linux desktop menu UI discovery, live
desktop OS paste-menu workflow proof, and Developer ID signed/notarized macOS
app distribution.
